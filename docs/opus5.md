❯ /model ⎿  Set model to Opus 5 (1M context) and saved as your default for new sessions

❯ "BIP157 실피어 통합 시험" 일을 16부대로 할 것. 이후 정확한 실측보고. 백그라운드

16부대 편성 전에, 전 부대가 공유할 node 전송 구현부터 제가 만들어 직렬 의존을 없앱니다:

● Write(scripts\btc-p2p\node-transport.mjs)
Wrote 90 lines to scripts\btc-p2p\node-transport.mjs
// node-transport.mjs — 실피어 시험용 ByteTransport 구현 (node net/tls).
//
// 왜 저장소에 두는가: BIP157 실피어 통합 시험을 여러 부대가 동시에 돌린다.
// 각자 소켓 코드를 새로 쓰면 측정값이 구현 차이로 흔들린다 — 전송은 하나로
// 고정하고, 부대는 프로토콜·측정만 다르게 한다.
//
// 계약: packages/wallet-sdk/src/btc-history/transport.ts 의 ByteTransport.
// 셸 구현(안드로이드 Capacitor·데스크톱 Tauri·확장 WS 릴레이)과 같은 표면이다.

import net from 'node:net';
… +80 lines

16부대 편성 완료. 전 부대 공유 전송을 먼저 깔아 직렬 의존을 제거했고(각 부대가 피어도 스스로 찾게 해서 서로 기다릴 일 없음), 쓰기 영역을 16개로 분리했습니다:

  Ran 1 shell command

● 16 background agents launched (↓ to manage)
1 DNS 시드 수집
2 서비스비트 피어 스캔
3 체크포인트 데이터
4 핸드셰이크 실측
5 cfheaders 실측
6 cfilters 실측
7 E2E 실주소 스캔
8 Electrum 교차대조
9 복수 피어 교차검증
10 재조직 롤백 시험
11 스캔 성능 프로파일
12 에러 경로 테스트
13 주소유형별 매칭
14 테스트넷 검증
15 적대적 코드리뷰
16 문서화

16부대 전원 투입 (04:02:46 기준). 측정 조건 하나 미리 밝힙니다: 이 기계 코어 16 → 동시 실행 상한 14, 즉 16개 중 2개는 대기열에서 시작합니다. 순수 16-way 병렬이 아니라 14+2 구조이므로 실측 보고 때 이 점을 반영해 계산하겠습니다.

┌─────┬───────────────────────────────────┬─────────────────────────────────┐
│  #  │               부대                │            쓰기 영역            │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 1   │ DNS 시드 수집                     │ scripts/btc-p2p/seeds.mjs       │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 2   │ 서비스비트(0x40) 피어 비율        │ service-scan.mjs                │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 3   │ 체크포인트 데이터                 │ bip157/checkpoints.ts           │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 4   │ 실피어 핸드셰이크 실측            │ measure-handshake.mjs           │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 5   │ cfheaders 수신 실측               │ measure-cfheaders.mjs           │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 6   │ cfilters 실측 + 전체 비용 계산    │ measure-cfilters.mjs            │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 7   │ E2E 실주소 스캔 (C의 핵심 미검증) │ e2e-scan.mjs                    │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 8   │ Electrum 교차 대조                │ cross-electrum.mjs              │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 9   │ 복수 피어 교차검증 + k-of-n       │ multi-peer.mjs                  │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 10  │ 재조직 롤백 테스트                │ tests/btc-bip157-reorg.test.ts  │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 11  │ 성능·메모리 프로파일              │ profile-scan.mjs                │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 12  │ 적대적 피어 에러 경로             │ tests/btc-bip157-errors.test.ts │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 13  │ 주소유형별 매칭(입력/출력 양방향) │ address-types.mjs               │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 14  │ testnet 삼각검증(실피어↔공식벡터) │ testnet-scan.mjs                │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 15  │ 적대적 코드리뷰 (쓰기 없음)       │ —                               │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 16  │ 인수인계 문서                     │ docs/BTC-HISTORY.md             │
└─────┴───────────────────────────────────┴─────────────────────────────────┘

전 부대에 "SDK 버그를 발견해도 고치지 말고 파일:라인으로 보고" 지시를 걸어 두었습니다 — 병  제가 통합 단계에서 일괄 처리합니다.

완료되는 대로 실측 수치와 함께 종합 보고하겠습니다.

✻ Brewed for 4m 40s

● Agent "2 서비스비트 피어 스캔" finished · 4m 25s

2번 부대 완료 (6분): 방화벽 차단 없음 — 실제 비트코인 P2P 접속이 이 기계에서 됩니다. 핵심 수치: DNS 시드 578개 주소 확보, 400회 시도 중 TCP 접속 84.3%·핸드셰이크 82.8% 성공, 그러나 compact filter(0x40)를
광고하는 피어는 17.5%뿐 — BIP157 을 쓰려면 피어 7회 시도당 1개꼴로 골라야 한다는 뜻입니다.

✻ Crunched for 10s

● Agent "11 스캔 성능 프로파일" finished · 5m 24s

11번 완료 (5.4분): 성능 병목이 SipHash 가 아니라 Golomb-Rice 디코더의 BigInt 로 특정됐습니다 — 같은 루프에서 BigInt 를 Number 로 바꾸면 7.5배 빨라진다는 격리 측정까지 나왔습니다(2,250µs → 302µs). 메모리 누수는 없고(1만 필터 처리 후 GC 뒤 힙 증가 0.02MB), 5만 블록 스캔은 필터 크기에 따라 0.5~3.8분·126MB~2.5GB 로 계산됐습니다. 최적화는 지시대로 손대지 않았으니 통합 때 제가 처리합니다.

✻ Sautéed for 7s

● Agent "16 문서화" finished · 6m 18s

16번 완료 (6.3분): 문서화 부대가 제 이전 보고의 과장 두 건을 잡아냈습니다.

- 제가 "58테스트 통과"라고 했지만, 그 58건은 전부 코덱·필터 계층이고 핵심인 scan.ts 466줄
- scripthash 4종 지원 중 P2TR 은 벡터 대조가 없습니다 (3종만 검증).
- BIP157 프라이버시도 "아무도 모른다"가 아니라 정확히는 "주소는 모르고, 관심 블록은 안다" — 매칭된 블록만 요청하기 때문입니다. 문서에 그렇게 정정해 적었습니다.

docs/BTC-HISTORY.md 에 검증 상태표가 "공식 벡터 검증 / 단위 테스트만 / 미검증"으로 갈라    진행 중.

✻ Churned for 15s

● Agent "1 DNS 시드 수집" finished · 9m 23s

1번 완료 (9.4분): 2번 부대 결과를 뒤집는 발견이 나왔습니다.

2번은 평범한 DNS 시드로 조회해 compact filter 피어가 17.5% 였는데, 1번은 x49 서비스비트   24 = 100% 가 0x40 을 광고했습니다. 같은 방식으로 평이름조회는 5/25 = 20%. 즉 BIP157 피어확보는 "7회 시도당 1개"가 아니라 "조회하면 전부" 입니다 — 접두어 하나 차이입니다.

덤으로 환경 함정도 하나 잡았습니다: 이 기계의 DNS 서버 중 1.1.1.1 이 막혀 있어, resolver 시드당 6~7초씩 헛기다립니다 (공유하면 0.14~1.0초). 초기 측정치의 5초는 전부 이아티팩트였다고 자체 정정 보고했습니다.

13개 부대 진행 중.

✻ Cooked for 15s

● Agent "4 핸드셰이크 실측" finished · 9m 16s

4번 완료 (9.3분): 실피어 핸드셰이크가 SDK 코드만으로 성립합니다 — 44/48 성공, 상대 44피어 전원이 프로토콜 70016·start height 960450 일치·compact filter 지원. ping/pong nonce 왕복 38/38, 상대가 먼저 보낸 ping
자동 응답 39/39.

버그도 4건 잡았는데, 그중 1건은 제가 쓴 파일입니다: 공유 전송(node-transport.mjs)에 setNoDelay 를 안 걸어 작은 패킷 연속 전송 시 왕복이 2배가 됩니다 — 같은 피어로 A/B 대조해 10개 중 9개에서 재현(519ms→259ms). scan 이 getcfheaders/getcfilters 를 연속으로 쏘는 구간에 그대로 걸립니다.

지금 5·6·7·8·9·13·14번 부대가 이 파일로 측정 중이라, 고치면 부대별 측정 조건이 갈립니다. 전원 귀환 후 수정하겠습니다.

또 하나 중요한 계약 결함: P2PFrameDecoder 가 넘겨받은 버퍼를 복사 없이 붙들고 있어서, 셸 전송 구현이 재사용 버퍼를 넘기면 스트림이 깨집니다. 제 node 구현은 복사를 해서 안 터지지만 안드로이드·데스크톱·릴레이
구현에서 터질 수 있는 자리입니다.

✻ Sautéed for 19s

● Agent "3 체크포인트 데이터" finished · 9m 34s

3번 완료 (9.6분): 체크포인트를 지어내지 않고 계산으로 확보했습니다. 검증 사슬이 특히 단단합니다 — 제네시스 코인베이스 원문이 BIP158 공식 벡터 블록 hex 와 바이트 일치함을 먼저 증명해 "기억에서 옮겨 적은 값이 아님"을 보이고, 같은 코드 경로로 계산한 testnet 제네시스 filterHeader 가 공식 벡터와 일치하는 것으로 mainnet 계산을 검산했습니다. 값 하나를 일부러 변조하면 스크립트가 exit 1 로 잡는 음성 테스트까지 확인.

mainnet 은 제네시스만 확보 — 10만 단위 체크포인트는 오프라인 검증 원천이 없어 빼고 "미확보

6개 완료, 10개 진행 중.

✻ Churned for 9s

● Agent "5 cfheaders 실측" finished · 10m 8s

5번 완료 (10.1분): SDK 디코더가 실제 네트워크 데이터로 완전 검증됐습니다 — 발견 문제 0건.

제네시스부터 현재 팁(960,450)까지 헤더 전량을 두 피어에서 받아 검사했습니다: prev 연결 끊김 0건, 작업증명(PoW) 검사 96만 건 전량 통과(디코더의 해시·난이도 필드가 정확하다는 증거), 두 피어 팁 해시 일치.

가장 값진 검증 하나: 하드코딩 상수 없이 제네시스에서 시작해 SDK 의 필터헤더 계산이 Bitcoin Core 자신의 필터헤더 체인과 일치함을 확인했습니다.

성능 실측: 근거리 피어에서 헤더 96만개 8.9초, 필터헤더 전체 동기 6.4초. 원거리 피어는 왕 량이 아니라 RTT 가 비용을 정한다는 게 수치로 나왔습니다.

7개 완료, 9개 진행 중.

✻ Waiting for 9 background agents to finish

● Agent "12 에러 경로 테스트" finished · 10m 7s

12번 완료 (10.1분): 취약점 3건 발견 — 적대적 피어가 지갑을 공격할 수 있는 자리들입니다.

1. 무한 요청 루프 (심각) — 피어가 같은 헤더 2000개를 계속 돌려주면 체인은 안 자라는데 요
2. 메시지 큐 무제한 (심각) — 아무도 안 기다리는 메시지가 큐에 영구 축적됩니다. 실측: 4KB 메시지 15,000통으로 64.6MB 메모리 증가 — 피어가 우리 프로세스 메모리를 원격 조종할 수 있습니다.
3. ping 파싱 예외 탈출 (중) — 0바이트 ping 하나로 소켓 콜백 밖으로 예외가 튀어 스캔이 영구 대기에 빠집니다.

방어가 잘 된 곳도 확인됐습니다: 페이로드 크기 상한 32MiB 존재(메모리 폭탄 차단), 피어 위  전부 예외 처리.

47개 테스트를 작성해 44개 통과, 3개는 취약점을 드러내며 의도적으로 실패하게 뒀습니다(지시  때 제가 고칩니다.

8개 완료, 8개 진행 중.

✻ Waiting for 8 background agents to finish

● Agent "15 적대적 코드리뷰" finished · 10m 20s

15번(적대적 리뷰) 완료 (10.3분): 가장 위험한 결함을 잡았습니다.

F1 — 내려받은 블록의 거래 목록이 머클루트로 검증되지 않습니다. 헤더 80바이트는 진짜인지 확 록은 피어가 마음대로 짜도 통과합니다. 결과: 피어가 사용자의 입금 거래 하나만 빼고 보내면 "이력 없음"으로 보이고 예외도 안 납니다. 반대로 없는 거래를 지어 넣을 수도 있습니다. 자금이 조용히 사라져 보이는 종류의 버그입니다.

같은 급으로 두 건 더: 필터 배치 회계가 "어느 블록인지"가 아니라 "몇 개인지"만 세서 구간이 , 체크포인트 이전에 받은 코인의 출금 이력이 기록되지 않습니다.

반대로 확인해준 것도 많습니다 — SipHash 구현을 벡터가 안 덮는 경계까지 손으로 추적해 정상 확인, GCS 병합 순회 4가지 경계 전부 정답, 바이트 순서·varint 경계·세그윗 파싱 정상, 그리고 셸 3종의 TLS 검증이 전부
완화 없이 엄격함을 확인했습니다.

9개 완료, 7개 진행 중.

✻ Waiting for 7 background agents to finish

● Agent "10 재조직 롤백 시험" finished · 11m 50s

10번 완료 (11.8분): 무한 루프 결함이 악성 피어가 아니라 정직한 Bitcoin Core 상대로도 터진다는 게 밝혀졌습니다. 원인은 locator 설계 — 끝 8개 + 체크포인트만 보내고 지수 back-off 가 없어서, 깊이 8 초과 재조직이
나면 정직한 피어가 "이미 아는 헤더 2000개"를 계속 돌려주고 스캔이 영원히 같은 요청을 반복  갑 + 깊은 재조직)은 이 코드가 존재하는 이유 그 자체입니다.

재조직 시험 13건 중 12건 통과 — 1블록·6블록 재조직에서 되감긴 블록이 필터·본문 요청 대상에 고, 1건은 위 결함을 드러내며 실패하게 뒀습니다.

10개 완료, 6개 진행 중.

✻ Waiting for 6 background agents to finish

● Agent "7 E2E 실주소 스캔" finished · 13m 2s

7번 완료 (13.0분): BIP157 전 과정이 실제 비트코인 네트워크에서 끝까지 동작합니다. C 부대   닫혔습니다.

검증 방식이 특히 단단합니다 — "필터가 맞다고 한 걸 필터로 검증하면 순환논증"이라며, 구간   .

- 피자 거래 구간: 그 유명한 10,000 BTC 수취를 찾아냈고, 그 코인이 다음 블록에서 지출된 것까지 추적했습니다 (받은 것만이 아니라 쓴 것도 잡힌다는 실물 증거).
- SegWit 시대 구간: 거래 53,498건 중 레코드 717건 전부 원문 재확인 통과, 놓친 것 0건.
- 거짓양성은 블록당 0.0637%로 정상 범위. 이 과정에서 기대값 계산도 실측에 맞춰 스스로 정정

속도: 피어를 미리 알면 전체 3.7초, 실제 스캔 자체는 83ms~1.8초. SDK 결함 발견 0건.

11개 완료, 5개 진행 중.

✻ Waiting for 5 background agents to finish

● Agent "13 주소유형별 매칭" finished · 16m 38s

13번 완료 (16.6분): 벼린 실제 요구사항 기준으로 검증했습니다.

- 주소 유형 5종(p2wpkh·p2tr·p2pkh·p2sh·p2wsh) 전부 매칭 성공 — 실제 최신 블록(거래 4,079건)에서 받기 8/8, 지출 쪽도 8/8. OP_RETURN 만 매칭 안 됨(BIP158 규격대로 정상).
- 벼린은 p2wpkh 단일 주소 모델(거스름돈도 자기 주소로 회수)이라 계정당 감시 스크립트 1개면 해집니다.
- p2tr 은 반쪽입니다: 주소 파생·수신·매칭은 되는데 buildTransfer 가 예외를 던져 받고 못 보냅니다.
- 15번 부대가 코드 판독으로 지적한 "출금 이력 누락"을 실측으로 재확인했습니다: 받은 블록이 스캔 범위 밖이면 필터는 매칭되는데 기록이 0건 — 게다가 이 누락과 단순 오탐이 결과만 봐서는 구분이 안 됩니다.
- 셸 배선 0건 — 이 기능은 아직 SDK 에만 있고 지갑 화면과 연결돼 있지 않다는 사실도 확인됐습니다.

12개 완료, 4개 진행 중.

✻ Waiting for 4 background agents to finish

● Agent "14 테스트넷 검증" finished · 16m 42s

14번 완료 (16.7분): 삼각 검증 성립 — BIP158 공식 규격 벡터 = 실제 Bitcoin Core 노드가 인 DK 디코더 해석, 셋이 8개 높이 전부 일치했습니다. 서로 다른 피어 3곳에서 각각 8/8. 빈필터(00)까지 정상 처리.

SDK 가 mainnet 에 하드코딩돼 있을 거라는 제 우려는 틀렸습니다 — 네트워크 매직이 인자로 빠져 있어 무수정으로 testnet 이 그대로 돌았습니다.

운영상 중요한 발견 하나: 0x40 을 광고해 놓고 실제로는 필터를 안 주는 피어가 있습니다 — 광고 9개 중 실제 서빙은 5개(55.6%). 응답도 안 하고 끊지도 않아 타임아웃까지 매답니다. 피어 선택 로직이 광고만 믿으면 안 되고 무응답 시 다음 피어로 넘어가야 한다는 뜻입니다.

13개 완료, 3개 진행 중 (cfilters 실측·Electrum 교차대조·복수 피어 검증).

✻ Waiting for 3 background agents to finish

● Agent "8 Electrum 교차대조" finished · 19m 43s

8번 완료 (19.7분): 두 트랙이 같은 답을 냅니다 — 5개 대조 창 중 4개에서 txid 집합 완전 일치(A창은 9회 반복 전부 8/8), 오탐 0.

정책 결정에 필요한 비용 수치가 나왔습니다:

┌───────────────────────┬──────────────────┬─────────────────────┐
│                       │     Electrum     │       BIP157        │
├───────────────────────┼──────────────────┼─────────────────────┤
│ 같은 1000블록 창 소요 │ 0.9초            │ 중앙값 7.4초 (~8배) │
├───────────────────────┼──────────────────┼─────────────────────┤
│ 수신 바이트           │ 533KB            │ 25.9MB (48.5배)     │
├───────────────────────┼──────────────────┼─────────────────────┤
│ 비용이 늘어나는 축    │ 주소의 이력 길이 │ 스캔 창 길이        │
└───────────────────────┴──────────────────┴─────────────────────┘

불일치 1건은 앞선 두 부대가 지적한 출금 이력 누락이 세 번째로 독립 재확인된 것입니다 — 필터는 블록을 맞혔고 블록도 받아왔는데, "내 코인이 어느 것인지" 모르는 상태라 기록이 안 만들어집니다.

부수 발견: Electrum 은 이력이 아주 긴 주소를 아예 거부합니다(Too many history entries) — BIP157 에는 그 한계가 없습니다. 그리고 체크포인트 값이 독립 피어 5대에서 동일함을 확인해 교차 검증도 성립했습니다.

14개 완료, 2개 진행 중.

✻ Waiting for 2 background agents to finish

● Agent "9 복수 피어 교차검증" finished · 21m 29s

9번 완료 (21.5분): 복수 피어 교차검증 — 89개 피어, 불일치 0건.

방법론이 특히 정직합니다: 최근 체크포인트를 하드코딩하지 않고 제네시스만 신뢰 상수로 두고 피어 3개가 각자 독립적으로 96만 헤더를 걸어 올라가 팁에 도달했습니다(3/3 만장일치). 그 앵커로 89개 피어에게 같은 구간을 물었더니 필터헤더·헤더 원문·팁 해시가 바이트 단위로 전부 동일 — Satoshi 23~31, Knots, btcd 까지 구현체가 갈려도 같았습니다.

스스로 한계도 명시했습니다: "정상 상태에서 다수결은 성립하지만 아무것도 걸러내지 못한다 — 이 실측은 악의적 피어의 존재를 증명도 반증도 못 한다. 측정한 건 교차검증이 오탐을 내지 않는다는 것뿐."

k-of-n 제안도 가정치를 밝힌 계산으로 냈습니다 — 적대 피어 10% 가정 시 응답 피어 4개 전원 일치면 오답 채택 확률 2.9e-4 (기대 다이얼 5.4회).

15개 완료, 마지막 1개(cfilters 실측) 진행 중입니다.

✻ Waiting for 1 background agent to finish

● Agent "6 cfilters 실측" finished · 27m 19s

16부대 전원 귀환. 실측 집계와 저장소 상태를 확인합니다:

  Pushed to main, listed 1 directory, ran 2 shell commands

16부대 실측 보고

시간 — 계산과 실측

투입 04:02:46 → 완료 04:31:27
사람 시계        28분 41초  (1,721초)
부대 시계 합    201분 42초  (12,101초)  ← 순차로 했다면 3시간 22분
압축비              7.03배
가장 느린 부대  27분 18초  (cfilters 실측)

계산이 예측한 대로 나왔습니다. 지난번에 "벽시계 = 가장 긴 직렬 사슬"이라고 계산했는데, 실제로 사람 시계(28.7분) ≈ 최장 부대(27.3분) + 투입·대기 1.4분입니다. 부대를 16개로 늘려도 가장 느린 하나보다 빨라지지 않습니다.

압축비가 16이 아니라 7인 이유 두 가지: ① 이 기계 코어가 16개라 동시 실행 상한이 14 — 2개는 대기열에서 시작 ② 부대별 소요가 4.4분~27.3분으로 6배 넘게 갈려 빨리 끝난 부대들이 놀았습니다. 더 빠르게 하려면 부대를
늘리는 게 아니라 가장 느린 부대를 쪼개야 합니다.

부수 실측: 도구 호출 645회, 소비 토큰 약 163만.

결과 — BIP157 은 실제로 동작합니다

C 부대가 "미검증"으로 남긴 항목이 전부 닫혔습니다.

- 실주소 E2E 성립: 피자 거래 10,000 BTC 수취를 찾고, 그 코인이 다음 블록에서 지출된 것까지 추적. SegWit 구간 717레코드 전량 원문 재확인, 놓친 것 0. 검증은 블록 원문을 따로 받아 만든 독립 정답과
대조했습니다(필터로 필터를 검증하는 순환논증 회피).
- 삼각 검증: BIP158 공식 벡터 = 실제 Bitcoin Core 노드 바이트 = 우리 디코더, 8개 높이 × 3피어 전부 일치.
- 헤더 96만개 작업증명 전량 통과, 89피어 교차 불일치 0, Electrum 과 txid 집합 4/5 창 완

그런데 — 자금이 조용히 사라지는 결함 3종

이게 이번 라운드의 진짜 수확입니다. 부대들이 서로 다른 각도에서 같은 곳을 찔렀습니다.

1. 블록 거래 목록이 머클루트로 검증되지 않습니다. 피어가 내 입금 거래 하나만 빼고 보내면 "이력 없음"이 되고 예외도 안 납니다. 없는 거래를 지어 넣을 수도 있습니다.
2. 헤더 요청이 무한 반복될 수 있습니다 — 악성 피어가 아니라 정직한 Bitcoin Core 상대로도 재현됩니다. 2주쯤 꺼둔 지갑 + 깊은 재조직이면 발동합니다.
3. 피어가 우리 메모리를 원격 조종할 수 있습니다 — 아무도 안 기다리는 메시지가 무제한 쌓입  ).

여기에 출금 이력 누락(부대 8·13·15가 각각 독립적으로 확인), ping 예외 탈출, 제 전송 파일

정책 결정에 쓸 수치

┌──────────────────┬───────────────────────┬─────────────────────────────┐
│                  │       Electrum        │           BIP157            │
├──────────────────┼───────────────────────┼─────────────────────────────┤
│ 같은 1000블록 창 │ 0.9초 · 533KB         │ 7.4초 · 25.9MB              │
├──────────────────┼───────────────────────┼─────────────────────────────┤
│ 전체 체인 필터   │ —                     │ 12.2GB · 1.9~6.5시간        │
├──────────────────┼───────────────────────┼─────────────────────────────┤
│ 프라이버시       │ 서버가 내 주소를 앎   │ 주소는 모름, 관심 블록은 앎 │
├──────────────────┼───────────────────────┼─────────────────────────────┤
│ 한계             │ 이력 긴 주소는 거부됨 │ 없음                        │
└──────────────────┴───────────────────────┴─────────────────────────────┘

보고해야 할 규칙 위반 하나

부대 중 하나가 지정 영역 밖에 scripts/bip157-live/ 를 만들었습니다. 내용은 검증된 실측(피어 10곳 병렬 스캔, 결과 전원 일치)이라 지우지 않고 커밋했지만, 영역 규칙을 어긴 것은 사실 그대로 보고합니다.

푸시 완료 1c78b20. 테스트 4건이 일부러 실패한 채 들어갔습니다 — 위 결함들을 기록한 것이   않았습니다. 이 코드는 아직 어느 셸에도 배선되지 않아 사용자 자금에 지금 영향은 없습니다.

다음은 결함 수정 라운드입니다. 지시 주시면 그 규모로 편성하겠습니다.
  ⎿  API error: 400 {"type":"error","error":{"type":"invalid_request_error","message":"modt 256 characters"},"request_id":"req_011Cdb1CgFGqySTJ2VkwYP3b"}

❯ /model ⎿  Set model to Opus 5 (1M context) and saved as your default for new sessions

❯ "BIP157 실피어 통합 시험" 일을 16부대로 할 것. 이후 정확한 실측보고. 백그라운드

16부대 편성 전에, 전 부대가 공유할 node 전송 구현부터 제가 만들어 직렬 의존을 없앱니다:

● Write(scripts\btc-p2p\node-transport.mjs)
Wrote 90 lines to scripts\btc-p2p\node-transport.mjs
// node-transport.mjs — 실피어 시험용 ByteTransport 구현 (node net/tls).
//
// 왜 저장소에 두는가: BIP157 실피어 통합 시험을 여러 부대가 동시에 돌린다.
// 각자 소켓 코드를 새로 쓰면 측정값이 구현 차이로 흔들린다 — 전송은 하나로
// 고정하고, 부대는 프로토콜·측정만 다르게 한다.
//
// 계약: packages/wallet-sdk/src/btc-history/transport.ts 의 ByteTransport.
// 셸 구현(안드로이드 Capacitor·데스크톱 Tauri·확장 WS 릴레이)과 같은 표면이다.

import net from 'node:net';
… +80 lines

16부대 편성 완료. 전 부대 공유 전송을 먼저 깔아 직렬 의존을 제거했고(각 부대가 피어도 스스 음), 쓰기 영역을 16개로 분리했습니다:

  Ran 1 shell command

● 16 background agents launched (↓ to manage)
1 DNS 시드 수집
2 서비스비트 피어 스캔
3 체크포인트 데이터
4 핸드셰이크 실측
5 cfheaders 실측
6 cfilters 실측
7 E2E 실주소 스캔
8 Electrum 교차대조
9 복수 피어 교차검증
10 재조직 롤백 시험
11 스캔 성능 프로파일
12 에러 경로 테스트
13 주소유형별 매칭
14 테스트넷 검증
15 적대적 코드리뷰
16 문서화

16부대 전원 투입 (04:02:46 기준). 측정 조건 하나 미리 밝힙니다: 이 기계 코어 16 → 동시 실행 상한 14, 즉 16개 중 2개는 대기열에서 시작합니다. 순수 16-way 병렬이 아니라 14+2 구조이므로 실측 보고 때 이 점을 반영해 계산하겠습니다.

┌─────┬───────────────────────────────────┬─────────────────────────────────┐
│  #  │               부대                │            쓰기 영역            │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 1   │ DNS 시드 수집                     │ scripts/btc-p2p/seeds.mjs       │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 2   │ 서비스비트(0x40) 피어 비율        │ service-scan.mjs                │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 3   │ 체크포인트 데이터                 │ bip157/checkpoints.ts           │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 4   │ 실피어 핸드셰이크 실측            │ measure-handshake.mjs           │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 5   │ cfheaders 수신 실측               │ measure-cfheaders.mjs           │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 6   │ cfilters 실측 + 전체 비용 계산    │ measure-cfilters.mjs            │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 7   │ E2E 실주소 스캔 (C의 핵심 미검증) │ e2e-scan.mjs                    │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 8   │ Electrum 교차 대조                │ cross-electrum.mjs              │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 9   │ 복수 피어 교차검증 + k-of-n       │ multi-peer.mjs                  │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 10  │ 재조직 롤백 테스트                │ tests/btc-bip157-reorg.test.ts  │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 11  │ 성능·메모리 프로파일              │ profile-scan.mjs                │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 12  │ 적대적 피어 에러 경로             │ tests/btc-bip157-errors.test.ts │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 13  │ 주소유형별 매칭(입력/출력 양방향) │ address-types.mjs               │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 14  │ testnet 삼각검증(실피어↔공식벡터) │ testnet-scan.mjs                │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 15  │ 적대적 코드리뷰 (쓰기 없음)       │ —                               │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 16  │ 인수인계 문서                     │ docs/BTC-HISTORY.md             │
└─────┴───────────────────────────────────┴─────────────────────────────────┘

전 부대에 "SDK 버그를 발견해도 고치지 말고 파일:라인으로 보고" 지시를 걸어 두었습니다 — 병렬 중 소스 충돌을 막고, 수정은 제가 통합 단계에서 일괄 처리합니다.

완료되는 대로 실측 수치와 함께 종합 보고하겠습니다.

✻ Brewed for 4m 40s

● Agent "2 서비스비트 피어 스캔" finished · 4m 25s

2번 부대 완료 (6분): 방화벽 차단 없음 — 실제 비트코인 P2P 접속이 이 기계에서 됩니다. 핵심 수치: DNS 시드 578개 주소 확보, 400회 시도 중 TCP 접속 84.3%·핸드셰이크 82.8% 성공, 그러나 compact filter(0x40)를 광고하는 피어는 17.5%뿐 — BIP157 을 쓰려면 피어 7회 시도당 1개꼴로 골라야 한다는 뜻입니다. 나머지 15개 부대 진행 중.

✻ Crunched for 10s

● Agent "11 스캔 성능 프로파일" finished · 5m 24s

11번 완료 (5.4분): 성능 병목이 SipHash 가 아니라 Golomb-Rice 디코더의 BigInt 로 특정됐습니다 — 같은 루프에서 BigInt 를 Number 로 바꾸면 7.5배 빨라진다는 격리 측정까지 나왔습니다(2,250µs → 302µs). 메모리
누수는 없고(1만 필터 처리 후 GC 뒤 힙 증가 0.02MB), 5만 블록 스캔은 필터 크기에 따라 0.5~3 다. 최적화는 지시대로 손대지 않았으니 통합 때 제가 처리합니다.

✻ Sautéed for 7s

● Agent "16 문서화" finished · 6m 18s

16번 완료 (6.3분): 문서화 부대가 제 이전 보고의 과장 두 건을 잡아냈습니다.

- 제가 "58테스트 통과"라고 했지만, 그 58건은 전부 코덱·필터 계층이고 핵심인 scan.ts 466줄은 자동 테스트가 0건입니다.
- scripthash 4종 지원 중 P2TR 은 벡터 대조가 없습니다 (3종만 검증).
- BIP157 프라이버시도 "아무도 모른다"가 아니라 정확히는 "주소는 모르고, 관심 블록은 안다" — 매칭된 블록만 요청하기 때문입니다. 문서에 그렇게 정정해 적었습니다.

docs/BTC-HISTORY.md 에 검증 상태표가 "공식 벡터 검증 / 단위 테스트만 / 미검증"으로 갈라    진행 중.

✻ Churned for 15s

● Agent "1 DNS 시드 수집" finished · 9m 23s

1번 완료 (9.4분): 2번 부대 결과를 뒤집는 발견이 나왔습니다.

2번은 평범한 DNS 시드로 조회해 compact filter 피어가 17.5% 였는데, 1번은 x49 서비스비트  터링 문법)를 써서 조회하니 24/24 = 100% 가 0x40 을 광고했습니다. 같은 방식으로 평이름조회는 5/25 = 20%. 즉 BIP157 피어 확보는 "7회 시도당 1개"가 아니라 "조회하면 전부" 입니다 — 접두어 하나 차이입니다.

덤으로 환경 함정도 하나 잡았습니다: 이 기계의 DNS 서버 중 1.1.1.1 이 막혀 있어, resolver 시드당 6~7초씩 헛기다립니다 (공유하면 0.14~1.0초). 초기 측정치의 5초는 전부 이아티팩트였다고 자체 정정 보고했습니다.

13개 부대 진행 중.

✻ Cooked for 15s

● Agent "4 핸드셰이크 실측" finished · 9m 16s

4번 완료 (9.3분): 실피어 핸드셰이크가 SDK 코드만으로 성립합니다 — 44/48 성공, 상대 44피어 전원이 프로토콜 70016·start height 960450 일치·compact filter 지원. ping/pong nonce 왕복 38/38, 상대가 먼저 보낸 ping 자동 응답 39/39.

버그도 4건 잡았는데, 그중 1건은 제가 쓴 파일입니다: 공유 전송(node-transport.mjs)에 setNoD  전송 시 왕복이 2배가 됩니다 — 같은 피어로 A/B 대조해 10개 중 9개에서 재현(519ms→259ms). scan 이 getcfheaders/getcfilters 를 연속으로 쏘는 구간에 그대로 걸립니다.

지금 5·6·7·8·9·13·14번 부대가 이 파일로 측정 중이라, 고치면 부대별 측정 조건이 갈립니다. 전원 귀환 후 수정하겠습니다.

또 하나 중요한 계약 결함: P2PFrameDecoder 가 넘겨받은 버퍼를 복사 없이 붙들고 있어서, 셸 전송 구현이 재사용 버퍼를 넘기면 스트림이 깨집니다. 제 node 구현은 복사를 해서 안 터지지만 안드로이드·데스크톱·릴레이
구현에서 터질 수 있는 자리입니다.

✻ Sautéed for 19s

● Agent "3 체크포인트 데이터" finished · 9m 34s

3번 완료 (9.6분): 체크포인트를 지어내지 않고 계산으로 확보했습니다. 검증 사슬이 특히 단단합니다 — 제네시스 코인베이스 원문이 BIP158 공식 벡터 블록 hex 와 바이트 일치함을 먼저 증명해 "기억에서 옮겨 적은 값이 아님"을 보이고, 같은 코드 경로로 계산한 testnet 제네시스 filterHeader 가 공식 벡터와 일치하는 것으로 mainnet 계산을 검산했습니다. 값 하나를 일부러 변조하면 스크립트가 exit 1 로 잡는 음성 테스트까지 확인.

mainnet 은 제네시스만 확보 — 10만 단위 체크포인트는 오프라인 검증 원천이 없어 빼고 "미확보"로 명시했습니다.

6개 완료, 10개 진행 중.

✻ Churned for 9s

● Agent "5 cfheaders 실측" finished · 10m 8s

5번 완료 (10.1분): SDK 디코더가 실제 네트워크 데이터로 완전 검증됐습니다 — 발견 문제 0건.

제네시스부터 현재 팁(960,450)까지 헤더 전량을 두 피어에서 받아 검사했습니다: prev 연결 끊김 0건, 작업증명(PoW) 검사 96만 건 전량 통과(디코더의 해시·난이도 필드가 정확하다는 증거), 두 피어 팁 해시 일치.

가장 값진 검증 하나: 하드코딩 상수 없이 제네시스에서 시작해 SDK 의 필터헤더 계산이 Bitcoin Core 자신의 필터헤더 체인과 일치함을 확인했습니다.

성능 실측: 근거리 피어에서 헤더 96만개 8.9초, 필터헤더 전체 동기 6.4초. 원거리 피어는 왕 량이 아니라 RTT 가 비용을 정한다는 게 수치로 나왔습니다.

7개 완료, 9개 진행 중.

✻ Waiting for 9 background agents to finish

● Agent "12 에러 경로 테스트" finished · 10m 7s

12번 완료 (10.1분): 취약점 3건 발견 — 적대적 피어가 지갑을 공격할 수 있는 자리들입니다.

1. 무한 요청 루프 (심각) — 피어가 같은 헤더 2000개를 계속 돌려주면 체인은 안 자라는데 요
2. 메시지 큐 무제한 (심각) — 아무도 안 기다리는 메시지가 큐에 영구 축적됩니다. 실측: 4KB 메시지 15,000통으로 64.6MB 메모리 증가 — 피어가 우리 프로세스 메모리를 원격 조종할 수 있습니다.
3. ping 파싱 예외 탈출 (중) — 0바이트 ping 하나로 소켓 콜백 밖으로 예외가 튀어 스캔이 영구 대기에 빠집니다.

방어가 잘 된 곳도 확인됐습니다: 페이로드 크기 상한 32MiB 존재(메모리 폭탄 차단), 피어 위

47개 테스트를 작성해 44개 통과, 3개는 취약점을 드러내며 의도적으로 실패하게 뒀습니다(지시대로 구현에 맞추지 않음). 통합 때 제가 고칩니다.

8개 완료, 8개 진행 중.

✻ Waiting for 8 background agents to finish

● Agent "15 적대적 코드리뷰" finished · 10m 20s

15번(적대적 리뷰) 완료 (10.3분): 가장 위험한 결함을 잡았습니다.

F1 — 내려받은 블록의 거래 목록이 머클루트로 검증되지 않습니다. 헤더 80바이트는 진짜인지 확인하는데, 그 뒤에 붙은 거래 목록은 피어가 마음대로 짜도 통과합니다. 결과: 피어가 사용자의 입금 거래 하나만 빼고 보내면 "이력 없음"으로 보이고 예외도 안 납니다. 반대로 없는 거래를 지어 넣을 수도 있습니다. 자금이 조용히 사라져 보이는 종류의 버그입니다.

같은 급으로 두 건 더: 필터 배치 회계가 "어느 블록인지"가 아니라 "몇 개인지"만 세서 구간이 통째로 누락될 수 있고(예외 0), 체크포인트 이전에 받은 코인의 출금 이력이 기록되지 않습니다.

반대로 확인해준 것도 많습니다 — SipHash 구현을 벡터가 안 덮는 경계까지 손으로 추적해 정상 확인, GCS 병합 순회 4가지 경계 전부 정답, 바이트 순서·varint 경계·세그윗 파싱 정상, 그리고 셸 3종의 TLS 검증이 전부
완화 없이 엄격함을 확인했습니다.

9개 완료, 7개 진행 중.

✻ Waiting for 7 background agents to finish

● Agent "10 재조직 롤백 시험" finished · 11m 50s

10번 완료 (11.8분): 무한 루프 결함이 악성 피어가 아니라 정직한 Bitcoin Core 상대로도 터진다는 게 밝혀졌습니다. 원인은 locator 설계 — 끝 8개 + 체크포인트만 보내고 지수 back-off 가 없어서, 깊이 8 초과 재조직이 나면 정직한 피어가 "이미 아는 헤더 2000개"를 계속 돌려주고 스캔이 영원히 같은 요청을 반복합니다. 발동 조건(2주쯤 꺼둔 지갑 + 깊은 재조직)은 이 코드가 존재하는 이유 그 자체입니다.

재조직 시험 13건 중 12건 통과 — 1블록·6블록 재조직에서 되감긴 블록이 필터·본문 요청 대상에서 정확히 빠지는 것까지 확인했고, 1건은 위 결함을 드러내며 실패하게 뒀습니다.

10개 완료, 6개 진행 중.

✻ Waiting for 6 background agents to finish

● Agent "7 E2E 실주소 스캔" finished · 13m 2s

7번 완료 (13.0분): BIP157 전 과정이 실제 비트코인 네트워크에서 끝까지 동작합니다. C 부대   닫혔습니다.

검증 방식이 특히 단단합니다 — "필터가 맞다고 한 걸 필터로 검증하면 순환논증"이라며, 구간   .

- 피자 거래 구간: 그 유명한 10,000 BTC 수취를 찾아냈고, 그 코인이 다음 블록에서 지출된 것까지 추적했습니다 (받은 것만이 아니라 쓴 것도 잡힌다는 실물 증거).
- SegWit 시대 구간: 거래 53,498건 중 레코드 717건 전부 원문 재확인 통과, 놓친 것 0건.
- 거짓양성은 블록당 0.0637%로 정상 범위. 이 과정에서 기대값 계산도 실측에 맞춰 스스로 정정

속도: 피어를 미리 알면 전체 3.7초, 실제 스캔 자체는 83ms~1.8초. SDK 결함 발견 0건.

11개 완료, 5개 진행 중.

✻ Waiting for 5 background agents to finish

● Agent "13 주소유형별 매칭" finished · 16m 38s

13번 완료 (16.6분): 벼린 실제 요구사항 기준으로 검증했습니다.

- 주소 유형 5종(p2wpkh·p2tr·p2pkh·p2sh·p2wsh) 전부 매칭 성공 — 실제 최신 블록(거래 4,079건)에서 받기 8/8, 지출 쪽도 8/8. OP_RETURN 만 매칭 안 됨(BIP158 규격대로 정상).
- 벼린은 p2wpkh 단일 주소 모델(거스름돈도 자기 주소로 회수)이라 계정당 감시 스크립트 1개면 충분합니다 — 스캔 설계가 단순해집니다.
- p2tr 은 반쪽입니다: 주소 파생·수신·매칭은 되는데 buildTransfer 가 예외를 던져 받고 못 보냅니다.
- 15번 부대가 코드 판독으로 지적한 "출금 이력 누락"을 실측으로 재확인했습니다: 받은 블록이 스캔 범위 밖이면 필터는 매칭되는데 기록이 0건 — 게다가 이 누락과 단순 오탐이 결과만 봐서는 구분이 안 됩니다.
- 셸 배선 0건 — 이 기능은 아직 SDK 에만 있고 지갑 화면과 연결돼 있지 않다는 사실도 확인됐

12개 완료, 4개 진행 중.

✻ Waiting for 4 background agents to finish

● Agent "14 테스트넷 검증" finished · 16m 42s

14번 완료 (16.7분): 삼각 검증 성립 — BIP158 공식 규격 벡터 = 실제 Bitcoin Core 노드가 인 DK 디코더 해석, 셋이 8개 높이 전부 일치했습니다. 서로 다른 피어 3곳에서 각각 8/8. 빈필터(00)까지 정상 처리.

SDK 가 mainnet 에 하드코딩돼 있을 거라는 제 우려는 틀렸습니다 — 네트워크 매직이 인자로 빠져 있어 무수정으로 testnet 이 그대로 돌았습니다.

운영상 중요한 발견 하나: 0x40 을 광고해 놓고 실제로는 필터를 안 주는 피어가 있습니다 — 광고 9개 중 실제 서빙은 5개(55.6%). 응답도 안 하고 끊지도 않아 타임아웃까지 매답니다. 피어 선택 로직이 광고만 믿으면 안 되고 무응답 시 다음 피어로 넘어가야 한다는 뜻입니다.

13개 완료, 3개 진행 중 (cfilters 실측·Electrum 교차대조·복수 피어 검증).

✻ Waiting for 3 background agents to finish

● Agent "8 Electrum 교차대조" finished · 19m 43s

8번 완료 (19.7분): 두 트랙이 같은 답을 냅니다 — 5개 대조 창 중 4개에서 txid 집합 완전 일치(A창은 9회 반복 전부 8/8), 오탐 0.

정책 결정에 필요한 비용 수치가 나왔습니다:

┌───────────────────────┬──────────────────┬─────────────────────┐
│                       │     Electrum     │       BIP157        │
├───────────────────────┼──────────────────┼─────────────────────┤
│ 같은 1000블록 창 소요 │ 0.9초            │ 중앙값 7.4초 (~8배) │
├───────────────────────┼──────────────────┼─────────────────────┤
│ 수신 바이트           │ 533KB            │ 25.9MB (48.5배)     │
├───────────────────────┼──────────────────┼─────────────────────┤
│ 비용이 늘어나는 축    │ 주소의 이력 길이 │ 스캔 창 길이        │
└───────────────────────┴──────────────────┴─────────────────────┘

불일치 1건은 앞선 두 부대가 지적한 출금 이력 누락이 세 번째로 독립 재확인된 것입니다 — 필터는 블록을 맞혔고 블록도 받아왔는데, "내 코인이 어느 것인지" 모르는 상태라 기록이 안 만들어집니다.

부수 발견: Electrum 은 이력이 아주 긴 주소를 아예 거부합니다(Too many history entries) — BIP157 에는 그 한계가 없습니다. 그리고 체크포인트 값이 독립 피어 5대에서 동일함을 확인해 교차 검증도 성립했습니다.

14개 완료, 2개 진행 중.

✻ Waiting for 2 background agents to finish

● Agent "9 복수 피어 교차검증" finished · 21m 29s

9번 완료 (21.5분): 복수 피어 교차검증 — 89개 피어, 불일치 0건.

방법론이 특히 정직합니다: 최근 체크포인트를 하드코딩하지 않고 제네시스만 신뢰 상수로 두고 피어 3개가 각자 독립적으로 96만 헤더를 걸어 올라가 팁에 도달했습니다(3/3 만장일치). 그 앵커로 89개 피어에게 같은 구간을 물었더니 필터헤더·헤더 원문·팁 해시가 바이트 단위로 전부 동일 — Satoshi 23~31, Knots, btcd 까지 구현체가 갈려도 같았습니다.

스스로 한계도 명시했습니다: "정상 상태에서 다수결은 성립하지만 아무것도 걸러내지 못한다 — 이 실측은 악의적 피어의 존재를 증명도 반증도 못 한다. 측정한 건 교차검증이 오탐을 내지 않는다는 것뿐."

k-of-n 제안도 가정치를 밝힌 계산으로 냈습니다 — 적대 피어 10% 가정 시 응답 피어 4개 전원  기대 다이얼 5.4회).

15개 완료, 마지막 1개(cfilters 실측) 진행 중입니다.

✻ Waiting for 1 background agent to finish

● Agent "6 cfilters 실측" finished · 27m 19s

16부대 전원 귀환. 실측 집계와 저장소 상태를 확인합니다:

  Pushed to main, listed 1 directory, ran 2 shell commands

16부대 실측 보고

시간 — 계산과 실측

투입 04:02:46 → 완료 04:31:27
사람 시계        28분 41초  (1,721초)
부대 시계 합    201분 42초  (12,101초)  ← 순차로 했다면 3시간 22분
압축비              7.03배
가장 느린 부대  27분 18초  (cfilters 실측)

계산이 예측한 대로 나왔습니다. 지난번에 "벽시계 = 가장 긴 직렬 사슬"이라고 계산했는데, 실제로 사람 시계(28.7분) ≈ 최장 부대(27.3분) + 투입·대기 1.4분입니다. 부대를 16개로 늘려도 가장 느린 하나보다 빨라지지 않습니다.

압축비가 16이 아니라 7인 이유 두 가지: ① 이 기계 코어가 16개라 동시 실행 상한이 14 — 2개는 가 4.4분~27.3분으로 6배 넘게 갈려 빨리 끝난 부대들이 놀았습니다. 더 빠르게 하려면 부대를 늘리는 게 아니라 가장 느린 부대를 쪼개야 합니다.

부수 실측: 도구 호출 645회, 소비 토큰 약 163만.

결과 — BIP157 은 실제로 동작합니다

C 부대가 "미검증"으로 남긴 항목이 전부 닫혔습니다.

- 실주소 E2E 성립: 피자 거래 10,000 BTC 수취를 찾고, 그 코인이 다음 블록에서 지출된 것까지 추적. SegWit 구간 717레코드 전량 원문 재확인, 놓친 것 0. 검증은 블록 원문을 따로 받아 만든 독립 정답과 대조했습니다(필터로 필터를 검증하는 순환논증 회피).
- 삼각 검증: BIP158 공식 벡터 = 실제 Bitcoin Core 노드 바이트 = 우리 디코더, 8개 높이 × 3피어 전부 일치.
- 헤더 96만개 작업증명 전량 통과, 89피어 교차 불일치 0, Electrum 과 txid 집합 4/5 창 완

그런데 — 자금이 조용히 사라지는 결함 3종

이게 이번 라운드의 진짜 수확입니다. 부대들이 서로 다른 각도에서 같은 곳을 찔렀습니다.

1. 블록 거래 목록이 머클루트로 검증되지 않습니다. 피어가 내 입금 거래 하나만 빼고 보내면 "이력 없음"이 되고 예외도 안 납니다. 없는 거래를 지어 넣을 수도 있습니다.
2. 헤더 요청이 무한 반복될 수 있습니다 — 악성 피어가 아니라 정직한 Bitcoin Core 상대로도 재현됩니다. 2주쯤 꺼둔 지갑 + 깊은 재조직이면 발동합니다.
3. 피어가 우리 메모리를 원격 조종할 수 있습니다 — 아무도 안 기다리는 메시지가 무제한 쌓입니다(4KB×15,000통 = 64.6MB 실측).

여기에 출금 이력 누락(부대 8·13·15가 각각 독립적으로 확인), ping 예외 탈출, 제 전송 파일

정책 결정에 쓸 수치

┌──────────────────┬───────────────────────┬─────────────────────────────┐
│                  │       Electrum        │           BIP157            │
├──────────────────┼───────────────────────┼─────────────────────────────┤
│ 같은 1000블록 창 │ 0.9초 · 533KB         │ 7.4초 · 25.9MB              │
├──────────────────┼───────────────────────┼─────────────────────────────┤
│ 전체 체인 필터   │ —                     │ 12.2GB · 1.9~6.5시간        │
├──────────────────┼───────────────────────┼─────────────────────────────┤
│ 프라이버시       │ 서버가 내 주소를 앎   │ 주소는 모름, 관심 블록은 앎 │
├──────────────────┼───────────────────────┼─────────────────────────────┤
│ 한계             │ 이력 긴 주소는 거부됨 │ 없음                        │
└──────────────────┴───────────────────────┴─────────────────────────────┘

보고해야 할 규칙 위반 하나

부대 중 하나가 지정 영역 밖에 scripts/bip157-live/ 를 만들었습니다. 내용은 검증된 실측(피어 10곳 병렬 스캔, 결과 전원 일치)이라 지우지 않고 커밋했지만, 영역 규칙을 어긴 것은 사실 그대로 보고합니다.

푸시 완료 1c78b20. 테스트 4건이 일부러 실패한 채 들어갔습니다 — 위 결함들을 기록한 것이   않았습니다. 이 코드는 아직 어느 셸에도 배선되지 않아 사용자 자금에 지금 영향은 없습니다.

다음은 결함 수정 라운드입니다. 지시 주시면 그 규모로 편성하겠습니다.
  ⎿  API error: 400 {"type":"error","error":{"type":"invalid_request_error","message":"modt 256 characters"},"request_id":"req_011CdbAniDbJYrSrB6LQTGq5"}

❯ /model
  ⎿  Set model to Opus 5 (1M context) and saved as your default for new sessions

❯ /model
  ⎿  Set model to Fable 5 and saved as your default for new sessions with low effort

❯ /model ⎿  Set model to Opus 5 (1M context) and saved as your default for new sessions

❯ "BIP157 실피어 통합 시험" 일을 16부대로 할 것. 이후 정확한 실측보고. 백그라운드

16부대 편성 전에, 전 부대가 공유할 node 전송 구현부터 제가 만들어 직렬 의존을 없앱니다:

● Write(scripts\btc-p2p\node-transport.mjs)
Wrote 90 lines to scripts\btc-p2p\node-transport.mjs
// node-transport.mjs — 실피어 시험용 ByteTransport 구현 (node net/tls).
//
// 왜 저장소에 두는가: BIP157 실피어 통합 시험을 여러 부대가 동시에 돌린다.
// 각자 소켓 코드를 새로 쓰면 측정값이 구현 차이로 흔들린다 — 전송은 하나로
// 고정하고, 부대는 프로토콜·측정만 다르게 한다.
//
// 계약: packages/wallet-sdk/src/btc-history/transport.ts 의 ByteTransport.
// 셸 구현(안드로이드 Capacitor·데스크톱 Tauri·확장 WS 릴레이)과 같은 표면이다.

import net from 'node:net';
… +80 lines

16부대 편성 완료. 전 부대 공유 전송을 먼저 깔아 직렬 의존을 제거했고(각 부대가 피어도 스스로 찾게 해서 서로 기다릴 일 없음), 쓰기 영역을 16개로 분리했습니다:

  Ran 1 shell command

● 16 background agents launched (↓ to manage)
1 DNS 시드 수집
2 서비스비트 피어 스캔
3 체크포인트 데이터
4 핸드셰이크 실측
5 cfheaders 실측
6 cfilters 실측
7 E2E 실주소 스캔
8 Electrum 교차대조
9 복수 피어 교차검증
10 재조직 롤백 시험
11 스캔 성능 프로파일
12 에러 경로 테스트
13 주소유형별 매칭
14 테스트넷 검증
15 적대적 코드리뷰
16 문서화

16부대 전원 투입 (04:02:46 기준). 측정 조건 하나 미리 밝힙니다: 이 기계 코어 16 → 동시 실행 상한 14, 즉 16개 중 2개는 대기열에서 시작합니다. 순수 16-way 병렬이 아니라 14+2 구조이므로 실측 보고 때 이 점을 반영해 계산하겠습니다.

┌─────┬───────────────────────────────────┬─────────────────────────────────┐
│  #  │               부대                │            쓰기 영역            │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 1   │ DNS 시드 수집                     │ scripts/btc-p2p/seeds.mjs       │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 2   │ 서비스비트(0x40) 피어 비율        │ service-scan.mjs                │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 3   │ 체크포인트 데이터                 │ bip157/checkpoints.ts           │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 4   │ 실피어 핸드셰이크 실측            │ measure-handshake.mjs           │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 5   │ cfheaders 수신 실측               │ measure-cfheaders.mjs           │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 6   │ cfilters 실측 + 전체 비용 계산    │ measure-cfilters.mjs            │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 7   │ E2E 실주소 스캔 (C의 핵심 미검증) │ e2e-scan.mjs                    │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 8   │ Electrum 교차 대조                │ cross-electrum.mjs              │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 9   │ 복수 피어 교차검증 + k-of-n       │ multi-peer.mjs                  │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 10  │ 재조직 롤백 테스트                │ tests/btc-bip157-reorg.test.ts  │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 11  │ 성능·메모리 프로파일              │ profile-scan.mjs                │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 12  │ 적대적 피어 에러 경로             │ tests/btc-bip157-errors.test.ts │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 13  │ 주소유형별 매칭(입력/출력 양방향) │ address-types.mjs               │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 14  │ testnet 삼각검증(실피어↔공식벡터) │ testnet-scan.mjs                │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 15  │ 적대적 코드리뷰 (쓰기 없음)       │ —                               │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 16  │ 인수인계 문서                     │ docs/BTC-HISTORY.md             │
└─────┴───────────────────────────────────┴─────────────────────────────────┘

전 부대에 "SDK 버그를 발견해도 고치지 말고 파일:라인으로 보고" 지시를 걸어 두었습니다 — 병렬 중 소스 충돌을 막고, 수정은 제가 통합 단계에서 일괄 처리합니다.

완료되는 대로 실측 수치와 함께 종합 보고하겠습니다.

✻ Brewed for 4m 40s

● Agent "2 서비스비트 피어 스캔" finished · 4m 25s

2번 부대 완료 (6분): 방화벽 차단 없음 — 실제 비트코인 P2P 접속이 이 기계에서 됩니다. 핵심  , 400회 시도 중 TCP 접속 84.3%·핸드셰이크 82.8% 성공, 그러나 compact filter(0x40)를광고하는 피어는 17.5%뿐 — BIP157 을 쓰려면 피어 7회 시도당 1개꼴로 골라야 한다는 뜻입니다. 나머지 15개 부대 진행 중.

✻ Crunched for 10s

● Agent "11 스캔 성능 프로파일" finished · 5m 24s

11번 완료 (5.4분): 성능 병목이 SipHash 가 아니라 Golomb-Rice 디코더의 BigInt 로 특정됐습니다 — 같은 루프에서 BigInt 를 Number 로 바꾸면 7.5배 빨라진다는 격리 측정까지 나왔습니다(2,250µs → 302µs). 메모리 누수는 없고(1만 필터 처리 후 GC 뒤 힙 증가 0.02MB), 5만 블록 스캔은 필터 크기에 따라 0.5~3.8분·126MB~2.5GB 로 계산됐습니다. 최적화는 지시대로 손대지 않았으니 통합 때 제가 처리합니다.

✻ Sautéed for 7s

● Agent "16 문서화" finished · 6m 18s

16번 완료 (6.3분): 문서화 부대가 제 이전 보고의 과장 두 건을 잡아냈습니다.

- 제가 "58테스트 통과"라고 했지만, 그 58건은 전부 코덱·필터 계층이고 핵심인 scan.ts 466줄은 자동 테스트가 0건입니다.
- scripthash 4종 지원 중 P2TR 은 벡터 대조가 없습니다 (3종만 검증).
- BIP157 프라이버시도 "아무도 모른다"가 아니라 정확히는 "주소는 모르고, 관심 블록은 안다" 입니다. 문서에 그렇게 정정해 적었습니다.

docs/BTC-HISTORY.md 에 검증 상태표가 "공식 벡터 검증 / 단위 테스트만 / 미검증"으로 갈라    진행 중.

✻ Churned for 15s

● Agent "1 DNS 시드 수집" finished · 9m 23s

1번 완료 (9.4분): 2번 부대 결과를 뒤집는 발견이 나왔습니다.

2번은 평범한 DNS 시드로 조회해 compact filter 피어가 17.5% 였는데, 1번은 x49 서비스비트  터링 문법)를 써서 조회하니 24/24 = 100% 가 0x40 을 광고했습니다. 같은 방식으로 평이름조회는 5/25 = 20%. 즉 BIP157 피어
확보는 "7회 시도당 1개"가 아니라 "조회하면 전부" 입니다 — 접두어 하나 차이입니다.

덤으로 환경 함정도 하나 잡았습니다: 이 기계의 DNS 서버 중 1.1.1.1 이 막혀 있어, resolver 시드당 6~7초씩 헛기다립니다 (공유하면 0.14~1.0초). 초기 측정치의 5초는 전부 이아티팩트였다고 자체 정정 보고했습니다.

13개 부대 진행 중.

✻ Cooked for 15s

● Agent "4 핸드셰이크 실측" finished · 9m 16s

4번 완료 (9.3분): 실피어 핸드셰이크가 SDK 코드만으로 성립합니다 — 44/48 성공, 상대 44피어 전원이 프로토콜 70016·start height 960450 일치·compact filter 지원. ping/pong nonce 왕복 38/38, 상대가 먼저 보낸 ping 자동 응답 39/39.

버그도 4건 잡았는데, 그중 1건은 제가 쓴 파일입니다: 공유 전송(node-transport.mjs)에 setNoD  전송 시 왕복이 2배가 됩니다 — 같은 피어로 A/B 대조해 10개 중 9개에서 재현(519ms→259ms). scan 이 getcfheaders/getcfilters 를 연속으로 쏘는 구간에 그대로 걸립니다.

지금 5·6·7·8·9·13·14번 부대가 이 파일로 측정 중이라, 고치면 부대별 측정 조건이 갈립니다. 전원 귀환 후 수정하겠습니다.

또 하나 중요한 계약 결함: P2PFrameDecoder 가 넘겨받은 버퍼를 복사 없이 붙들고 있어서, 셸 전송 구현이 재사용 버퍼를 넘기면 스트림이 깨집니다. 제 node 구현은 복사를 해서 안 터지지만 안드로이드·데스크톱·릴레이
구현에서 터질 수 있는 자리입니다.

✻ Sautéed for 19s

● Agent "3 체크포인트 데이터" finished · 9m 34s

3번 완료 (9.6분): 체크포인트를 지어내지 않고 계산으로 확보했습니다. 검증 사슬이 특히 단단합니다 — 제네시스 코인베이스 원문이 BIP158 공식 벡터 블록 hex 와 바이트 일치함을 먼저 증명해 "기억에서 옮겨 적은 값이 아님"을 보이고, 같은 코드 경로로 계산한 testnet 제네시스 filterHeader 가 공식 벡터와 일치하는 것으로 mainnet 계산을 검산했습니다. 값 하나를 일부러 변조하면 스크립트가 exit 1 로 잡는 음성 테스트까지 확인.

mainnet 은 제네시스만 확보 — 10만 단위 체크포인트는 오프라인 검증 원천이 없어 빼고 "미확보"로 명시했습니다.

6개 완료, 10개 진행 중.

✻ Churned for 9s

● Agent "5 cfheaders 실측" finished · 10m 8s

5번 완료 (10.1분): SDK 디코더가 실제 네트워크 데이터로 완전 검증됐습니다 — 발견 문제 0건.

제네시스부터 현재 팁(960,450)까지 헤더 전량을 두 피어에서 받아 검사했습니다: prev 연결 끊김 0건, 작업증명(PoW) 검사 96만 건 전량 통과(디코더의 해시·난이도 필드가 정확하다는 증거), 두 피어 팁 해시 일치.

가장 값진 검증 하나: 하드코딩 상수 없이 제네시스에서 시작해 SDK 의 필터헤더 계산이 Bitcoin Core 자신의 필터헤더 체인과 일치함을 확인했습니다.

성능 실측: 근거리 피어에서 헤더 96만개 8.9초, 필터헤더 전체 동기 6.4초. 원거리 피어는 왕 량이 아니라 RTT 가 비용을 정한다는 게 수치로 나왔습니다.

7개 완료, 9개 진행 중.

✻ Waiting for 9 background agents to finish

● Agent "12 에러 경로 테스트" finished · 10m 7s

12번 완료 (10.1분): 취약점 3건 발견 — 적대적 피어가 지갑을 공격할 수 있는 자리들입니다.

1. 무한 요청 루프 (심각) — 피어가 같은 헤더 2000개를 계속 돌려주면 체인은 안 자라는데 요
2. 메시지 큐 무제한 (심각) — 아무도 안 기다리는 메시지가 큐에 영구 축적됩니다. 실측: 4KB 메시지 15,000통으로 64.6MB 메모리 증가 — 피어가 우리 프로세스 메모리를 원격 조종할 수 있습니다.
3. ping 파싱 예외 탈출 (중) — 0바이트 ping 하나로 소켓 콜백 밖으로 예외가 튀어 스캔이 영구 대기에 빠집니다.

방어가 잘 된 곳도 확인됐습니다: 페이로드 크기 상한 32MiB 존재(메모리 폭탄 차단), 피어 위  전부 예외 처리.

47개 테스트를 작성해 44개 통과, 3개는 취약점을 드러내며 의도적으로 실패하게 뒀습니다(지시  때 제가 고칩니다.

8개 완료, 8개 진행 중.

✻ Waiting for 8 background agents to finish

● Agent "15 적대적 코드리뷰" finished · 10m 20s

15번(적대적 리뷰) 완료 (10.3분): 가장 위험한 결함을 잡았습니다.

F1 — 내려받은 블록의 거래 목록이 머클루트로 검증되지 않습니다. 헤더 80바이트는 진짜인지 확인하는데, 그 뒤에 붙은 거래 목록은 피어가 마음대로 짜도 통과합니다. 결과: 피어가 사용자의 입금 거래 하나만 빼고 보내면 "이력 없음"으로 보이고 예외도 안 납니다. 반대로 없는 거래를 지어 넣을 수도 있습니다. 자금이 조용히 사라져 보이는 종류의 버그입니다.

같은 급으로 두 건 더: 필터 배치 회계가 "어느 블록인지"가 아니라 "몇 개인지"만 세서 구간이 통째로 누락될 수 있고(예외 0), 체크포인트 이전에 받은 코인의 출금 이력이 기록되지 않습니다.

반대로 확인해준 것도 많습니다 — SipHash 구현을 벡터가 안 덮는 경계까지 손으로 추적해 정상   전부 정답, 바이트 순서·varint 경계·세그윗 파싱 정상, 그리고 셸 3종의 TLS 검증이 전부
완화 없이 엄격함을 확인했습니다.

9개 완료, 7개 진행 중.

✻ Waiting for 7 background agents to finish

● Agent "10 재조직 롤백 시험" finished · 11m 50s

10번 완료 (11.8분): 무한 루프 결함이 악성 피어가 아니라 정직한 Bitcoin Core 상대로도 터진다는 게 밝혀졌습니다. 원인은 locator 설계 — 끝 8개 + 체크포인트만 보내고 지수 back-off 가 없어서, 깊이 8 초과 재조직이 나면 정직한 피어가 "이미 아는 헤더 2000개"를 계속 돌려주고 스캔이 영원히 같은 요청을 반복합니다. 발동 조건(2주쯤 꺼둔 지갑 + 깊은 재조직)은 이 코드가 존재하는 이유 그 자체입니다.

재조직 시험 13건 중 12건 통과 — 1블록·6블록 재조직에서 되감긴 블록이 필터·본문 요청 대상에서 정확히 빠지는 것까지 확인했고, 1건은 위 결함을 드러내며 실패하게 뒀습니다.

10개 완료, 6개 진행 중.

✻ Waiting for 6 background agents to finish

● Agent "7 E2E 실주소 스캔" finished · 13m 2s

7번 완료 (13.0분): BIP157 전 과정이 실제 비트코인 네트워크에서 끝까지 동작합니다. C 부대   닫혔습니다.

검증 방식이 특히 단단합니다 — "필터가 맞다고 한 걸 필터로 검증하면 순환논증"이라며, 구간  립 정답을 만든 뒤 대조했습니다.

- 피자 거래 구간: 그 유명한 10,000 BTC 수취를 찾아냈고, 그 코인이 다음 블록에서 지출된 것  아니라 쓴 것도 잡힌다는 실물 증거).
- SegWit 시대 구간: 거래 53,498건 중 레코드 717건 전부 원문 재확인 통과, 놓친 것 0건.
- 거짓양성은 블록당 0.0637%로 정상 범위. 이 과정에서 기대값 계산도 실측에 맞춰 스스로 정정했습니다.

속도: 피어를 미리 알면 전체 3.7초, 실제 스캔 자체는 83ms~1.8초. SDK 결함 발견 0건.

11개 완료, 5개 진행 중.

✻ Waiting for 5 background agents to finish

● Agent "13 주소유형별 매칭" finished · 16m 38s

13번 완료 (16.6분): 벼린 실제 요구사항 기준으로 검증했습니다.

- 주소 유형 5종(p2wpkh·p2tr·p2pkh·p2sh·p2wsh) 전부 매칭 성공 — 실제 최신 블록(거래 4,079건. OP_RETURN 만 매칭 안 됨(BIP158 규격대로 정상).
- 벼린은 p2wpkh 단일 주소 모델(거스름돈도 자기 주소로 회수)이라 계정당 감시 스크립트 1개면 충분합니다 — 스캔 설계가 단순해집니다.
- p2tr 은 반쪽입니다: 주소 파생·수신·매칭은 되는데 buildTransfer 가 예외를 던져 받고 못 보냅니다.
- 15번 부대가 코드 판독으로 지적한 "출금 이력 누락"을 실측으로 재확인했습니다: 받은 블록이 스캔 범위 밖이면 필터는 매칭되는데 기록이 0건 — 게다가 이 누락과 단순 오탐이 결과만 봐서는 구분이 안 됩니다.
- 셸 배선 0건 — 이 기능은 아직 SDK 에만 있고 지갑 화면과 연결돼 있지 않다는 사실도 확인됐습니다.

12개 완료, 4개 진행 중.

✻ Waiting for 4 background agents to finish

● Agent "14 테스트넷 검증" finished · 16m 42s

14번 완료 (16.7분): 삼각 검증 성립 — BIP158 공식 규격 벡터 = 실제 Bitcoin Core 노드가 인 D전부 일치했습니다. 서로 다른 피어 3곳에서 각각 8/8. 빈필터(00)까지 정상 처리.

SDK 가 mainnet 에 하드코딩돼 있을 거라는 제 우려는 틀렸습니다 — 네트워크 매직이 인자로 빠져 있어 무수정으로 testnet 이 그대로 돌았습니다.

운영상 중요한 발견 하나: 0x40 을 광고해 놓고 실제로는 필터를 안 주는 피어가 있습니다 — 광고 9개 중 실제 서빙은 5개(55.6%). 응답도 안 하고 끊지도 않아 타임아웃까지 매답니다. 피어 선택 로직이 광고만 믿으면 안 되고 무응답 시 다음 피어로 넘어가야 한다는 뜻입니다.

13개 완료, 3개 진행 중 (cfilters 실측·Electrum 교차대조·복수 피어 검증).

✻ Waiting for 3 background agents to finish

● Agent "8 Electrum 교차대조" finished · 19m 43s

8번 완료 (19.7분): 두 트랙이 같은 답을 냅니다 — 5개 대조 창 중 4개에서 txid 집합 완전 일치(A창은 9회 반복 전부 8/8), 오탐 0.

정책 결정에 필요한 비용 수치가 나왔습니다:

┌───────────────────────┬──────────────────┬─────────────────────┐
│                       │     Electrum     │       BIP157        │
├───────────────────────┼──────────────────┼─────────────────────┤
│ 같은 1000블록 창 소요 │ 0.9초            │ 중앙값 7.4초 (~8배) │
├───────────────────────┼──────────────────┼─────────────────────┤
│ 수신 바이트           │ 533KB            │ 25.9MB (48.5배)     │
├───────────────────────┼──────────────────┼─────────────────────┤
│ 비용이 늘어나는 축    │ 주소의 이력 길이 │ 스캔 창 길이        │
└───────────────────────┴──────────────────┴─────────────────────┘

불일치 1건은 앞선 두 부대가 지적한 출금 이력 누락이 세 번째로 독립 재확인된 것입니다 — 필터는 블록을 맞혔고 블록도 받아왔는데, "내 코인이 어느 것인지" 모르는 상태라 기록이 안 만들어집니다.

부수 발견: Electrum 은 이력이 아주 긴 주소를 아예 거부합니다(Too many history entries) — BIP157 에는 그 한계가 없습니다. 그리고 체크포인트 값이 독립 피어 5대에서 동일함을 확인해 교차 검증도 성립했습니다.

14개 완료, 2개 진행 중.

✻ Waiting for 2 background agents to finish

● Agent "9 복수 피어 교차검증" finished · 21m 29s

9번 완료 (21.5분): 복수 피어 교차검증 — 89개 피어, 불일치 0건.

방법론이 특히 정직합니다: 최근 체크포인트를 하드코딩하지 않고 제네시스만 신뢰 상수로 두고 피어 3개가 각자 독립적으로 96만 헤더를 걸어 올라가 팁에 도달했습니다(3/3 만장일치). 그 앵커로 89개 피어에게 같은 구간을 물었더니 필터헤더·헤더 원문·팁 해시가 바이트 단위로 전부 동일 — Satoshi 23~31, Knots, btcd 까지 구현체가 갈려도 같았습니다.

스스로 한계도 명시했습니다: "정상 상태에서 다수결은 성립하지만 아무것도 걸러내지 못한다 —   증명도 반증도 못 한다. 측정한 건 교차검증이 오탐을 내지 않는다는 것뿐."

k-of-n 제안도 가정치를 밝힌 계산으로 냈습니다 — 적대 피어 10% 가정 시 응답 피어 4개 전원 일치면 오답 채택 확률 2.9e-4 (기대 다이얼 5.4회).

15개 완료, 마지막 1개(cfilters 실측) 진행 중입니다.

✻ Waiting for 1 background agent to finish

● Agent "6 cfilters 실측" finished · 27m 19s

16부대 전원 귀환. 실측 집계와 저장소 상태를 확인합니다:

  Pushed to main, listed 1 directory, ran 2 shell commands

16부대 실측 보고

시간 — 계산과 실측

투입 04:02:46 → 완료 04:31:27
사람 시계        28분 41초  (1,721초)
부대 시계 합    201분 42초  (12,101초)  ← 순차로 했다면 3시간 22분
압축비              7.03배
가장 느린 부대  27분 18초  (cfilters 실측)

계산이 예측한 대로 나왔습니다. 지난번에 "벽시계 = 가장 긴 직렬 사슬"이라고 계산했는데, 실제로 사람 시계(28.7분) ≈ 최장 부대(27.3분) + 투입·대기 1.4분입니다. 부대를 16개로 늘려도 가장 느린 하나보다 빨라지지 않습니다.

압축비가 16이 아니라 7인 이유 두 가지: ① 이 기계 코어가 16개라 동시 실행 상한이 14 — 2개는 대기열에서 시작 ② 부대별 소요가 4.4분~27.3분으로 6배 넘게 갈려 빨리 끝난 부대들이 놀았습니다. 더 빠르게 하려면 부대를 늘리는 게 아니라 가장 느린 부대를 쪼개야 합니다.

부수 실측: 도구 호출 645회, 소비 토큰 약 163만.

결과 — BIP157 은 실제로 동작합니다

C 부대가 "미검증"으로 남긴 항목이 전부 닫혔습니다.

- 실주소 E2E 성립: 피자 거래 10,000 BTC 수취를 찾고, 그 코인이 다음 블록에서 지출된 것까지전량 원문 재확인, 놓친 것 0. 검증은 블록 원문을 따로 받아 만든 독립 정답과대조했습니다(필터로 필터를 검증하는 순환논증 회피).
- 삼각 검증: BIP158 공식 벡터 = 실제 Bitcoin Core 노드 바이트 = 우리 디코더, 8개 높이 × 3피어 전부 일치.
- 헤더 96만개 작업증명 전량 통과, 89피어 교차 불일치 0, Electrum 과 txid 집합 4/5 창 완

그런데 — 자금이 조용히 사라지는 결함 3종

이게 이번 라운드의 진짜 수확입니다. 부대들이 서로 다른 각도에서 같은 곳을 찔렀습니다.

1. 블록 거래 목록이 머클루트로 검증되지 않습니다. 피어가 내 입금 거래 하나만 빼고 보내면 " 니다. 없는 거래를 지어 넣을 수도 있습니다.
2. 헤더 요청이 무한 반복될 수 있습니다 — 악성 피어가 아니라 정직한 Bitcoin Core 상대로도 재현됩니다. 2주쯤 꺼둔 지갑 + 깊은 재조직이면 발동합니다.
3. 피어가 우리 메모리를 원격 조종할 수 있습니다 — 아무도 안 기다리는 메시지가 무제한 쌓입니다(4KB×15,000통 = 64.6MB 실측).

여기에 출금 이력 누락(부대 8·13·15가 각각 독립적으로 확인), ping 예외 탈출, 제 전송 파일

정책 결정에 쓸 수치

┌──────────────────┬───────────────────────┬─────────────────────────────┐
│                  │       Electrum        │           BIP157            │
├──────────────────┼───────────────────────┼─────────────────────────────┤
│ 같은 1000블록 창 │ 0.9초 · 533KB         │ 7.4초 · 25.9MB              │
├──────────────────┼───────────────────────┼─────────────────────────────┤
│ 전체 체인 필터   │ —                     │ 12.2GB · 1.9~6.5시간        │
├──────────────────┼───────────────────────┼─────────────────────────────┤
│ 프라이버시       │ 서버가 내 주소를 앎   │ 주소는 모름, 관심 블록은 앎 │
├──────────────────┼───────────────────────┼─────────────────────────────┤
│ 한계             │ 이력 긴 주소는 거부됨 │ 없음                        │
└──────────────────┴───────────────────────┴─────────────────────────────┘

보고해야 할 규칙 위반 하나

부대 중 하나가 지정 영역 밖에 scripts/bip157-live/ 를 만들었습니다. 내용은 검증된 실측(피어 10곳 병렬 스캔, 결과 전원 일치)이라 지우지 않고 커밋했지만, 영역 규칙을 어긴 것은 사실 그대로 보고합니다.

푸시 완료 1c78b20. 테스트 4건이 일부러 실패한 채 들어갔습니다 — 위 결함들을 기록한 것이   느 셸에도 배선되지 않아 사용자 자금에 지금 영향은 없습니다.

다음은 결함 수정 라운드입니다. 지시 주시면 그 규모로 편성하겠습니다.
  ⎿  API error: 400 {"type":"error","error":{"type":"invalid_request_error","message":"model: String should have at most 256 characters"},"request_id":"req_011CdbAs4uu9dPc37SoDifRH"}

❯ /login
  ⎿  Login successful

❯ /model
  ⎿  Set model to Fable 5 and saved as your default for new sessions

❯ /model ⎿  Set model to Opus 5 (1M context) and saved as your default for new sessions

❯ "BIP157 실피어 통합 시험" 일을 16부대로 할 것. 이후 정확한 실측보고. 백그라운드

16부대 편성 전에, 전 부대가 공유할 node 전송 구현부터 제가 만들어 직렬 의존을 없앱니다:

● Write(scripts\btc-p2p\node-transport.mjs)
Wrote 90 lines to scripts\btc-p2p\node-transport.mjs
// node-transport.mjs — 실피어 시험용 ByteTransport 구현 (node net/tls).
//
// 왜 저장소에 두는가: BIP157 실피어 통합 시험을 여러 부대가 동시에 돌린다.
// 각자 소켓 코드를 새로 쓰면 측정값이 구현 차이로 흔들린다 — 전송은 하나로
// 고정하고, 부대는 프로토콜·측정만 다르게 한다.
//
// 계약: packages/wallet-sdk/src/btc-history/transport.ts 의 ByteTransport.
// 셸 구현(안드로이드 Capacitor·데스크톱 Tauri·확장 WS 릴레이)과 같은 표면이다.

import net from 'node:net';
… +80 lines

16부대 편성 완료. 전 부대 공유 전송을 먼저 깔아 직렬 의존을 제거했고(각 부대가 피어도 스스로 찾게 해서 서로 기다릴 일 없음), 쓰기 영역을 16개로 분리했습니다:

  Ran 1 shell command

● 16 background agents launched (↓ to manage)
1 DNS 시드 수집
2 서비스비트 피어 스캔
3 체크포인트 데이터
4 핸드셰이크 실측
5 cfheaders 실측
6 cfilters 실측
7 E2E 실주소 스캔
8 Electrum 교차대조
9 복수 피어 교차검증
10 재조직 롤백 시험
11 스캔 성능 프로파일
12 에러 경로 테스트
13 주소유형별 매칭
14 테스트넷 검증
15 적대적 코드리뷰
16 문서화

16부대 전원 투입 (04:02:46 기준). 측정 조건 하나 미리 밝힙니다: 이 기계 코어 16 → 동시 실행 상한 14, 즉 16개 중 2개는 대기열에서 시작합니다. 순수 16-way 병렬이 아니라 14+2 구조이므로 실측 보고 때 이 점을 반영해 계산하겠습니다.

┌─────┬───────────────────────────────────┬─────────────────────────────────┐
│  #  │               부대                │            쓰기 영역            │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 1   │ DNS 시드 수집                     │ scripts/btc-p2p/seeds.mjs       │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 2   │ 서비스비트(0x40) 피어 비율        │ service-scan.mjs                │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 3   │ 체크포인트 데이터                 │ bip157/checkpoints.ts           │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 4   │ 실피어 핸드셰이크 실측            │ measure-handshake.mjs           │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 5   │ cfheaders 수신 실측               │ measure-cfheaders.mjs           │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 6   │ cfilters 실측 + 전체 비용 계산    │ measure-cfilters.mjs            │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 7   │ E2E 실주소 스캔 (C의 핵심 미검증) │ e2e-scan.mjs                    │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 8   │ Electrum 교차 대조                │ cross-electrum.mjs              │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 9   │ 복수 피어 교차검증 + k-of-n       │ multi-peer.mjs                  │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 10  │ 재조직 롤백 테스트                │ tests/btc-bip157-reorg.test.ts  │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 11  │ 성능·메모리 프로파일              │ profile-scan.mjs                │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 12  │ 적대적 피어 에러 경로             │ tests/btc-bip157-errors.test.ts │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 13  │ 주소유형별 매칭(입력/출력 양방향) │ address-types.mjs               │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 14  │ testnet 삼각검증(실피어↔공식벡터) │ testnet-scan.mjs                │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 15  │ 적대적 코드리뷰 (쓰기 없음)       │ —                               │
├─────┼───────────────────────────────────┼─────────────────────────────────┤
│ 16  │ 인수인계 문서                     │ docs/BTC-HISTORY.md             │
└─────┴───────────────────────────────────┴─────────────────────────────────┘

전 부대에 "SDK 버그를 발견해도 고치지 말고 파일:라인으로 보고" 지시를 걸어 두었습니다 — 병렬 중 소스 충돌을 막고, 수정은 제가 통합 단계에서 일괄 처리합니다.

완료되는 대로 실측 수치와 함께 종합 보고하겠습니다.

✻ Brewed for 4m 40s

● Agent "2 서비스비트 피어 스캔" finished · 4m 25s

2번 부대 완료 (6분): 방화벽 차단 없음 — 실제 비트코인 P2P 접속이 이 기계에서 됩니다. 핵심 수치: DNS 시드 578개 주소 확보, 400회 시도 중 TCP 접속 84.3%·핸드셰이크 82.8% 성공, 그러나 compact filter(0x40)를 광고하는 피어는 17.5%뿐 — BIP157 을 쓰려면 피어 7회 시도당 1개꼴로 골라야 한다는 뜻입니다. 나머지 15개 부대 진행 중.

✻ Crunched for 10s

● Agent "11 스캔 성능 프로파일" finished · 5m 24s

11번 완료 (5.4분): 성능 병목이 SipHash 가 아니라 Golomb-Rice 디코더의 BigInt 로 특정됐습니다 — 같은 루프에서 BigInt 를 Number 로 바꾸면 7.5배 빨라진다는 격리 측정까지 나왔습니다(2,250µs → 302µs). 메모리
누수는 없고(1만 필터 처리 후 GC 뒤 힙 증가 0.02MB), 5만 블록 스캔은 필터 크기에 따라 0.5~3 다. 최적화는 지시대로 손대지 않았으니 통합 때 제가 처리합니다.

✻ Sautéed for 7s

● Agent "16 문서화" finished · 6m 18s

16번 완료 (6.3분): 문서화 부대가 제 이전 보고의 과장 두 건을 잡아냈습니다.

- 제가 "58테스트 통과"라고 했지만, 그 58건은 전부 코덱·필터 계층이고 핵심인 scan.ts 466줄은 자동 테스트가 0건입니다.
- scripthash 4종 지원 중 P2TR 은 벡터 대조가 없습니다 (3종만 검증).
- BIP157 프라이버시도 "아무도 모른다"가 아니라 정확히는 "주소는 모르고, 관심 블록은 안다" — 매칭된 블록만 요청하기 때문입니다. 문서에 그렇게 정정해 적었습니다.

docs/BTC-HISTORY.md 에 검증 상태표가 "공식 벡터 검증 / 단위 테스트만 / 미검증"으로 갈라    진행 중.

✻ Churned for 15s

● Agent "1 DNS 시드 수집" finished · 9m 23s

1번 완료 (9.4분): 2번 부대 결과를 뒤집는 발견이 나왔습니다.

2번은 평범한 DNS 시드로 조회해 compact filter 피어가 17.5% 였는데, 1번은 x49 서비스비트  터링 문법)를 써서 조회하니 24/24 = 100% 가 0x40 을 광고했습니다. 같은 방식으로 평이름조회는 5/25 = 20%. 즉 BIP157 피어 확보는 "7회 시도당 1개"가 아니라 "조회하면 전부" 입니다 — 접두어 하나 차이입니다.

덤으로 환경 함정도 하나 잡았습니다: 이 기계의 DNS 서버 중 1.1.1.1 이 막혀 있어, resolver   유하면 0.14~1.0초). 초기 측정치의 5초는 전부 이아티팩트였다고 자체 정정 보고했습니다.

13개 부대 진행 중.

✻ Cooked for 15s

● Agent "4 핸드셰이크 실측" finished · 9m 16s

4번 완료 (9.3분): 실피어 핸드셰이크가 SDK 코드만으로 성립합니다 — 44/48 성공, 상대 44피어 전원이 프로토콜 70016·start height 960450 일치·compact filter 지원. ping/pong nonce 왕복 38/38, 상대가 먼저 보낸 ping 자동 응답 39/39.

버그도 4건 잡았는데, 그중 1건은 제가 쓴 파일입니다: 공유 전송(node-transport.mjs)에 setNoDelay 를 안 걸어 작은 패킷 연속 전송 시 왕복이 2배가 됩니다 — 같은 피어로 A/B 대조해 10개 중 9개에서 재현(519ms→259ms). scan 이 getcfheaders/getcfilters 를 연속으로 쏘는 구간에 그대로 걸립니다.

지금 5·6·7·8·9·13·14번 부대가 이 파일로 측정 중이라, 고치면 부대별 측정 조건이 갈립니다. 전원 귀환 후 수정하겠습니다.

또 하나 중요한 계약 결함: P2PFrameDecoder 가 넘겨받은 버퍼를 복사 없이 붙들고 있어서, 셸  면 스트림이 깨집니다. 제 node 구현은 복사를 해서 안 터지지만 안드로이드·데스크톱·릴레이
구현에서 터질 수 있는 자리입니다.

✻ Sautéed for 19s

● Agent "3 체크포인트 데이터" finished · 9m 34s

3번 완료 (9.6분): 체크포인트를 지어내지 않고 계산으로 확보했습니다. 검증 사슬이 특히 단단합니다 — 제네시스 코인베이스 원문이 BIP158 공식 벡터 블록 hex 와 바이트 일치함을 먼저 증명해 "기억에서 옮겨 적은 값이 아님"을 보이고, 같은 코드 경로로 계산한 testnet 제네시스 filterHeader 가 공식 벡터와 일치하는 것으로 mainnet 계산을 검산했습니다. 값 하나를 일부러 변조하면 스크립트가 exit 1 로 잡는 음성 테스트까지 확인.

mainnet 은 제네시스만 확보 — 10만 단위 체크포인트는 오프라인 검증 원천이 없어 빼고 "미확보"로 명시했습니다.

6개 완료, 10개 진행 중.

✻ Churned for 9s

● Agent "5 cfheaders 실측" finished · 10m 8s

5번 완료 (10.1분): SDK 디코더가 실제 네트워크 데이터로 완전 검증됐습니다 — 발견 문제 0건.

제네시스부터 현재 팁(960,450)까지 헤더 전량을 두 피어에서 받아 검사했습니다: prev 연결 끊김 0건, 작업증명(PoW) 검사 96만 건 전량 통과(디코더의 해시·난이도 필드가 정확하다는 증거), 두 피어 팁 해시 일치.

가장 값진 검증 하나: 하드코딩 상수 없이 제네시스에서 시작해 SDK 의 필터헤더 계산이 Bitcoin Core 자신의 필터헤더 체인과 일치함을 확인했습니다.

성능 실측: 근거리 피어에서 헤더 96만개 8.9초, 필터헤더 전체 동기 6.4초. 원거리 피어는 왕 량이 아니라 RTT 가 비용을 정한다는 게 수치로 나왔습니다.

7개 완료, 9개 진행 중.

✻ Waiting for 9 background agents to finish

● Agent "12 에러 경로 테스트" finished · 10m 7s

12번 완료 (10.1분): 취약점 3건 발견 — 적대적 피어가 지갑을 공격할 수 있는 자리들입니다.

1. 무한 요청 루프 (심각) — 피어가 같은 헤더 2000개를 계속 돌려주면 체인은 안 자라는데 요 검사도 라운드 상한도 없습니다.
2. 메시지 큐 무제한 (심각) — 아무도 안 기다리는 메시지가 큐에 영구 축적됩니다. 실측: 4KB 메시지 15,000통으로 64.6MB 메모리 증가 — 피어가 우리 프로세스 메모리를 원격 조종할 수 있습니다.
3. ping 파싱 예외 탈출 (중) — 0바이트 ping 하나로 소켓 콜백 밖으로 예외가 튀어 스캔이 영구 대기에 빠집니다.

방어가 잘 된 곳도 확인됐습니다: 페이로드 크기 상한 32MiB 존재(메모리 폭탄 차단), 피어 위  전부 예외 처리.

47개 테스트를 작성해 44개 통과, 3개는 취약점을 드러내며 의도적으로 실패하게 뒀습니다(지시  때 제가 고칩니다.

8개 완료, 8개 진행 중.

✻ Waiting for 8 background agents to finish

● Agent "15 적대적 코드리뷰" finished · 10m 20s

15번(적대적 리뷰) 완료 (10.3분): 가장 위험한 결함을 잡았습니다.

F1 — 내려받은 블록의 거래 목록이 머클루트로 검증되지 않습니다. 헤더 80바이트는 진짜인지 확인하는데, 그 뒤에 붙은 거래 목록은 피어가 마음대로 짜도 통과합니다. 결과: 피어가 사용자의 입금 거래 하나만 빼고 보내면
"이력 없음"으로 보이고 예외도 안 납니다. 반대로 없는 거래를 지어 넣을 수도 있습니다. 자금  버그입니다.

같은 급으로 두 건 더: 필터 배치 회계가 "어느 블록인지"가 아니라 "몇 개인지"만 세서 구간이 통째로 누락될 수 있고(예외 0), 체크포인트 이전에 받은 코인의 출금 이력이 기록되지 않습니다.

반대로 확인해준 것도 많습니다 — SipHash 구현을 벡터가 안 덮는 경계까지 손으로 추적해 정상 확인, GCS 병합 순회 4가지 경계 전부 정답, 바이트 순서·varint 경계·세그윗 파싱 정상, 그리고 셸 3종의 TLS 검증이 전부
완화 없이 엄격함을 확인했습니다.

9개 완료, 7개 진행 중.

✻ Waiting for 7 background agents to finish

● Agent "10 재조직 롤백 시험" finished · 11m 50s

10번 완료 (11.8분): 무한 루프 결함이 악성 피어가 아니라 정직한 Bitcoin Core 상대로도 터진다는 게 밝혀졌습니다. 원인은 locator 설계 — 끝 8개 + 체크포인트만 보내고 지수 back-off 가 없어서, 깊이 8 초과 재조직이 나면 정직한 피어가 "이미 아는 헤더 2000개"를 계속 돌려주고 스캔이 영원히 같은 요청을 반복합니다. 발동 조건(2주쯤 꺼둔 지갑 + 깊은 재조직)은 이 코드가 존재하는 이유 그 자체입니다.

재조직 시험 13건 중 12건 통과 — 1블록·6블록 재조직에서 되감긴 블록이 필터·본문 요청 대상에서 정확히 빠지는 것까지 확인했고, 1건은 위 결함을 드러내며 실패하게 뒀습니다.

10개 완료, 6개 진행 중.

✻ Waiting for 6 background agents to finish

● Agent "7 E2E 실주소 스캔" finished · 13m 2s

7번 완료 (13.0분): BIP157 전 과정이 실제 비트코인 네트워크에서 끝까지 동작합니다. C 부대   닫혔습니다.

검증 방식이 특히 단단합니다 — "필터가 맞다고 한 걸 필터로 검증하면 순환논증"이라며, 구간  립 정답을 만든 뒤 대조했습니다.

- 피자 거래 구간: 그 유명한 10,000 BTC 수취를 찾아냈고, 그 코인이 다음 블록에서 지출된 것까지 추적했습니다 (받은 것만이 아니라 쓴 것도 잡힌다는 실물 증거).
- SegWit 시대 구간: 거래 53,498건 중 레코드 717건 전부 원문 재확인 통과, 놓친 것 0건.
- 거짓양성은 블록당 0.0637%로 정상 범위. 이 과정에서 기대값 계산도 실측에 맞춰 스스로 정정했습니다.

속도: 피어를 미리 알면 전체 3.7초, 실제 스캔 자체는 83ms~1.8초. SDK 결함 발견 0건.

11개 완료, 5개 진행 중.

✻ Waiting for 5 background agents to finish

● Agent "13 주소유형별 매칭" finished · 16m 38s

13번 완료 (16.6분): 벼린 실제 요구사항 기준으로 검증했습니다.

- 주소 유형 5종(p2wpkh·p2tr·p2pkh·p2sh·p2wsh) 전부 매칭 성공 — 실제 최신 블록(거래 4,079건. OP_RETURN 만 매칭 안 됨(BIP158 규격대로 정상).
- 벼린은 p2wpkh 단일 주소 모델(거스름돈도 자기 주소로 회수)이라 계정당 감시 스크립트 1개면 충분합니다 — 스캔 설계가 단순해집니다.
- p2tr 은 반쪽입니다: 주소 파생·수신·매칭은 되는데 buildTransfer 가 예외를 던져 받고 못 보냅니다.
- 15번 부대가 코드 판독으로 지적한 "출금 이력 누락"을 실측으로 재확인했습니다: 받은 블록이되는데 기록이 0건 — 게다가 이 누락과 단순 오탐이 결과만 봐서는 구분이 안 됩니다.
- 셸 배선 0건 — 이 기능은 아직 SDK 에만 있고 지갑 화면과 연결돼 있지 않다는 사실도 확인됐습니다.

12개 완료, 4개 진행 중.

✻ Waiting for 4 background agents to finish

● Agent "14 테스트넷 검증" finished · 16m 42s

14번 완료 (16.7분): 삼각 검증 성립 — BIP158 공식 규격 벡터 = 실제 Bitcoin Core 노드가 인 DK 디코더 해석, 셋이 8개 높이 전부 일치했습니다. 서로 다른 피어 3곳에서 각각 8/8. 빈필터(00)까지 정상 처리.

SDK 가 mainnet 에 하드코딩돼 있을 거라는 제 우려는 틀렸습니다 — 네트워크 매직이 인자로 빠져 있어 무수정으로 testnet 이 그대로 돌았습니다.

운영상 중요한 발견 하나: 0x40 을 광고해 놓고 실제로는 필터를 안 주는 피어가 있습니다 — 광고 9개 중 실제 서빙은 5개(55.6%). 응답도 안 하고 끊지도 않아 타임아웃까지 매답니다. 피어 선택 로직이 광고만 믿으면 안 되고 무응답 시 다음 피어로 넘어가야 한다는 뜻입니다.

13개 완료, 3개 진행 중 (cfilters 실측·Electrum 교차대조·복수 피어 검증).

✻ Waiting for 3 background agents to finish

● Agent "8 Electrum 교차대조" finished · 19m 43s

8번 완료 (19.7분): 두 트랙이 같은 답을 냅니다 — 5개 대조 창 중 4개에서 txid 집합 완전 일치(A창은 9회 반복 전부 8/8), 오탐 0.

정책 결정에 필요한 비용 수치가 나왔습니다:

┌───────────────────────┬──────────────────┬─────────────────────┐
│                       │     Electrum     │       BIP157        │
├───────────────────────┼──────────────────┼─────────────────────┤
│ 같은 1000블록 창 소요 │ 0.9초            │ 중앙값 7.4초 (~8배) │
├───────────────────────┼──────────────────┼─────────────────────┤
│ 수신 바이트           │ 533KB            │ 25.9MB (48.5배)     │
├───────────────────────┼──────────────────┼─────────────────────┤
│ 비용이 늘어나는 축    │ 주소의 이력 길이 │ 스캔 창 길이        │
└───────────────────────┴──────────────────┴─────────────────────┘

불일치 1건은 앞선 두 부대가 지적한 출금 이력 누락이 세 번째로 독립 재확인된 것입니다 — 필터는 블록을 맞혔고 블록도 받아왔는데, "내 코인이 어느 것인지" 모르는 상태라 기록이 안 만들어집니다.

부수 발견: Electrum 은 이력이 아주 긴 주소를 아예 거부합니다(Too many history entries) — BIP157 에는 그 한계가 없습니다. 그리고 체크포인트 값이 독립 피어 5대에서 동일함을 확인해 교차 검증도 성립했습니다.

14개 완료, 2개 진행 중.

✻ Waiting for 2 background agents to finish

● Agent "9 복수 피어 교차검증" finished · 21m 29s

9번 완료 (21.5분): 복수 피어 교차검증 — 89개 피어, 불일치 0건.

방법론이 특히 정직합니다: 최근 체크포인트를 하드코딩하지 않고 제네시스만 신뢰 상수로 두고 피어 3개가 각자 독립적으로 96만 헤더를 걸어 올라가 팁에 도달했습니다(3/3 만장일치). 그 앵커로 89개 피어에게 같은 구간을 물었더니 필터헤더·헤더 원문·팁 해시가 바이트 단위로 전부 동일 — Satoshi 23~31, Knots, btcd 까지 구현체가 갈려도 같았습니다.

스스로 한계도 명시했습니다: "정상 상태에서 다수결은 성립하지만 아무것도 걸러내지 못한다 —   증명도 반증도 못 한다. 측정한 건 교차검증이 오탐을 내지 않는다는 것뿐."

k-of-n 제안도 가정치를 밝힌 계산으로 냈습니다 — 적대 피어 10% 가정 시 응답 피어 4개 전원 일치면 오답 채택 확률 2.9e-4 (기대 다이얼 5.4회).

15개 완료, 마지막 1개(cfilters 실측) 진행 중입니다.

✻ Waiting for 1 background agent to finish

● Agent "6 cfilters 실측" finished · 27m 19s

16부대 전원 귀환. 실측 집계와 저장소 상태를 확인합니다:

  Pushed to main, listed 1 directory, ran 2 shell commands

16부대 실측 보고

시간 — 계산과 실측

투입 04:02:46 → 완료 04:31:27
사람 시계        28분 41초  (1,721초)
부대 시계 합    201분 42초  (12,101초)  ← 순차로 했다면 3시간 22분
압축비              7.03배
가장 느린 부대  27분 18초  (cfilters 실측)

계산이 예측한 대로 나왔습니다. 지난번에 "벽시계 = 가장 긴 직렬 사슬"이라고 계산했는데, 실 부대(27.3분) + 투입·대기 1.4분입니다. 부대를 16개로 늘려도 가장 느린 하나보다 빨라지지않습니다.

압축비가 16이 아니라 7인 이유 두 가지: ① 이 기계 코어가 16개라 동시 실행 상한이 14 — 2개는 가 4.4분~27.3분으로 6배 넘게 갈려 빨리 끝난 부대들이 놀았습니다. 더 빠르게 하려면 부대를 늘리는 게 아니라 가장 느린 부대를 쪼개야 합니다.

부수 실측: 도구 호출 645회, 소비 토큰 약 163만.

결과 — BIP157 은 실제로 동작합니다

C 부대가 "미검증"으로 남긴 항목이 전부 닫혔습니다.

- 실주소 E2E 성립: 피자 거래 10,000 BTC 수취를 찾고, 그 코인이 다음 블록에서 지출된 것까지 추적. SegWit 구간 717레코드 전량 원문 재확인, 놓친 것 0. 검증은 블록 원문을 따로 받아 만든 독립 정답과 대조했습니다(필터로 필터를 검증하는 순환논증 회피).
- 삼각 검증: BIP158 공식 벡터 = 실제 Bitcoin Core 노드 바이트 = 우리 디코더, 8개 높이 × 3피어 전부 일치.
- 헤더 96만개 작업증명 전량 통과, 89피어 교차 불일치 0, Electrum 과 txid 집합 4/5 창 완

그런데 — 자금이 조용히 사라지는 결함 3종

이게 이번 라운드의 진짜 수확입니다. 부대들이 서로 다른 각도에서 같은 곳을 찔렀습니다.

1. 블록 거래 목록이 머클루트로 검증되지 않습니다. 피어가 내 입금 거래 하나만 빼고 보내면 "이력 없음"이 되고 예외도 안 납니다. 없는 거래를 지어 넣을 수도 있습니다.
2. 헤더 요청이 무한 반복될 수 있습니다 — 악성 피어가 아니라 정직한 Bitcoin Core 상대로도 재현됩니다. 2주쯤 꺼둔 지갑 + 깊은 재조직이면 발동합니다.
3. 피어가 우리 메모리를 원격 조종할 수 있습니다 — 아무도 안 기다리는 메시지가 무제한 쌓입니다(4KB×15,000통 = 64.6MB 실측).

여기에 출금 이력 누락(부대 8·13·15가 각각 독립적으로 확인), ping 예외 탈출, 제 전송 파일

정책 결정에 쓸 수치

┌──────────────────┬───────────────────────┬─────────────────────────────┐
│                  │       Electrum        │           BIP157            │
├──────────────────┼───────────────────────┼─────────────────────────────┤
│ 같은 1000블록 창 │ 0.9초 · 533KB         │ 7.4초 · 25.9MB              │
├──────────────────┼───────────────────────┼─────────────────────────────┤
│ 전체 체인 필터   │ —                     │ 12.2GB · 1.9~6.5시간        │
├──────────────────┼───────────────────────┼─────────────────────────────┤
│ 프라이버시       │ 서버가 내 주소를 앎   │ 주소는 모름, 관심 블록은 앎 │
├──────────────────┼───────────────────────┼─────────────────────────────┤
│ 한계             │ 이력 긴 주소는 거부됨 │ 없음                        │
└──────────────────┴───────────────────────┴─────────────────────────────┘

보고해야 할 규칙 위반 하나

부대 중 하나가 지정 영역 밖에 scripts/bip157-live/ 를 만들었습니다. 내용은 검증된 실측(피어 10곳 병렬 스캔, 결과 전원 일치)이라 지우지 않고 커밋했지만, 영역 규칙을 어긴 것은 사실 그대로 보고합니다.

푸시 완료 1c78b20. 테스트 4건이 일부러 실패한 채 들어갔습니다 — 위 결함들을 기록한 것이   느 셸에도 배선되지 않아 사용자 자금에 지금 영향은 없습니다.

다음은 결함 수정 라운드입니다. 지시 주시면 그 규모로 편성하겠습니다.