# 벼린 ← TTL 메모 기능 연동 명세

TTL 체인·인덱서·익스플로러 쪽 메모 기능이 **배포 완료**됐다. 벼린이 메모를 실어
보내고 되읽으려면 이 문서의 규칙을 그대로 따르면 된다.

* 작성 기준일: 2026-07-31
* 표기: `[실측]` = 이 문서 작성 중 직접 확인한 값 / `[미확인]` = 확인하지 못한 것
* **벼린 저장소 코드는 보지 않았다.** 아래는 인터페이스 계약이지 구현 지시가 아니다.

***

## 1. 한 줄 요약

메모는 **트랜잭션의** **`data`** **필드에 UTF-8 바이트로** 넣는다. 별도 컨트랙트도, 별도
트랜잭션도 없다. 평범한 송금 tx 하나에 `data` 만 붙이면 된다.

```
to     : 받는 사람 EOA
value  : 보낼 금액
data   : 0x + UTF-8(메모).toString('hex')     ← 이것만 추가
gas    : 21000 + (바이트당 16)                 ← 반드시 올려야 한다
```

***

## 2. 메모로 인정되는 조건

인덱서가 `data` 를 메모로 판정하는 규칙이다. **하나라도 어긋나면 메모가 아니라
기계 데이터로 분류되어 화면에 안 뜬다.** 서버 구현은 `wallet-api/memo.js` 다.

| 조건    | 값                                       |
| ----- | --------------------------------------- |
| 최소 길이 | **2 바이트** 이상                            |
| 최대 길이 | **2048 바이트** 이하                         |
| 인코딩   | **유효한 UTF-8** (엄격 디코드 — 깨진 바이트 하나면 탈락)  |
| 제어문자  | **불가**. 단 탭(`\t`)·개행(`\n`)·복귀(`\r`)는 허용 |
| 공백만   | 불가 (`trim()` 후 비면 탈락)                   |
| 형식    | `0x` 접두 + 짝수 길이 hex                     |

2048 바이트가 실제로 얼마인가 `[실측]`:

| 문자                | 바이트/자 | 2048B 에 들어가는 글자 수 |
| ----------------- | ----: | ----------------: |
| ASCII (영문·숫자·URL) |     1 |       **2,048 자** |
| 한글                |     3 |         **682 자** |
| 이모지               |     4 |             512 자 |

URL 은 길어야 200\~300 바이트, 계약서 제목은 한글 50자여도 150 바이트다. 제목 +
참조번호 + 짧은 설명을 같이 넣어도 남는다.

### 2048 바이트를 넘으면

메모로 인정되지 않아 `memo` 가 `null` 이 된다. 다만 `input_size` 에는 **자르기 전
실제 바이트 수**가 남으므로, 화면에서 "메모가 너무 김(N 바이트)" 으로 안내할 수 있다.

***

## 3. 인코딩 — 이 함수와 결과가 같아야 한다

서버 판정과 어긋나지 않도록, 보내는 쪽에서도 같은 검사를 먼저 하는 것을 권한다.

```js
/** 메모 문자열 → tx.data 로 넣을 hex. 규칙 위반이면 예외. */
function encodeMemo(text) {
  const bytes = new TextEncoder().encode(text);      // UTF-8
  if (bytes.length < 2)    throw new Error('메모가 너무 짧다 (2바이트 이상)');
  if (bytes.length > 2048) throw new Error(`메모가 너무 길다 (${bytes.length}/2048 바이트)`);
  if (text.trim().length === 0) throw new Error('공백만 있는 메모');
  for (const ch of text) {
    const c = ch.codePointAt(0);
    if (c === 0x09 || c === 0x0a || c === 0x0d) continue;   // tab, LF, CR
    if (c < 0x20 || (c >= 0x7f && c <= 0x9f)) throw new Error('제어문자 포함');
  }
  return '0x' + Buffer.from(bytes).toString('hex');
}
```

브라우저/RN 이면 `Buffer` 대신:

```js
'0x' + Array.from(bytes, (b) => b.toString(16).padStart(2, '0')).join('')
```

### 되읽기

받아온 hex 를 표시할 때도 같은 규칙을 통과시켜야 깨진 문자가 안 뜬다.

```js
function decodeMemo(hex) {
  if (!hex || !hex.startsWith('0x') || hex.length % 2 !== 0) return null;
  const bytes = Uint8Array.from(hex.slice(2).match(/../g)?.map((h) => parseInt(h, 16)) ?? []);
  if (bytes.length < 2 || bytes.length > 2048) return null;
  try {
    const text = new TextDecoder('utf-8', { fatal: true }).decode(bytes);   // fatal 필수
    if (!text.trim()) return null;
    for (const ch of text) {
      const c = ch.codePointAt(0);
      if (c === 0x09 || c === 0x0a || c === 0x0d) continue;
      if (c < 0x20 || (c >= 0x7f && c <= 0x9f) || c === 0xfffd) return null;
    }
    return text;
  } catch { return null; }
}
```

`fatal: true` 가 핵심이다. 이게 없으면 기계 데이터가 \`\` 로 뒤덮인 문자열로
디코드되어 화면에 쏟아진다.

***

## 4. 가스 — 반드시 올려야 한다

기본 송금 가스 한도 21,000 을 그대로 두면 **메모가 붙는 순간 tx 가 실패한다.**

계산식 (EIP-2028):

```
gas = 21,000 + (0 바이트 수 × 4) + (0 아닌 바이트 수 × 16)
```

한글·URL 은 사실상 전부 0 아닌 바이트이므로 **바이트당 16** 으로 잡으면 된다.

TTL 체인 실측값 `[실측]` — 블록 가스 한도 `55,000,000`, 가스 가격 `50 gwei`:

|      메모 크기 |        한글 |         가스 |              수수료 | 기본 송금 대비 |
| ---------: | --------: | ---------: | ---------------: | -------: |
|       36 B |      12 자 |     21,576 |     0.001079 TTL |     1.0배 |
|      256 B |      85 자 |     25,096 |     0.001255 TTL |     1.2배 |
|      512 B |     170 자 |     29,192 |     0.001460 TTL |     1.4배 |
|     1024 B |     341 자 |     37,384 |     0.001869 TTL |     1.8배 |
| **2048 B** | **682 자** | **53,768** | **0.002688 TTL** | **2.6배** |

2048 바이트 tx 로도 블록당 1,022 건이 들어가므로 처리량 문제는 없다 `[실측]`.

**권장**: 가스 한도를 하드코딩하지 말고 `estimateGas` 에 **data 를 포함해서** 부른다.
data 를 빼고 추정하면 21,000 이 나와서 실제 전송이 OOG 로 죽는다.

***

## 5. 되읽기 — 어느 API 를 부를 것인가

| 용도        | 엔드포인트                                                             | 메모 필드                                      |
| --------- | ----------------------------------------------------------------- | ------------------------------------------ |
| 주소별 활동 내역 | `GET https://api.ttl1.top/api/v1/address/{addr}/txs?page=&limit=` | `memo` (디코드된 텍스트), `input_size` (원본 바이트 수) |
| tx 상세     | `GET https://scan.ttl1.top/api/tx/{hash}`                         | `data` (원본 hex 전체)                         |

두 경로의 성격이 다르다 `[실측]`:

* `api.ttl1.top` 은 **인덱서 DB** 를 읽는다. `memo` 가 이미 텍스트라 그대로 표시하면 된다.
* `scan.ttl1.top/api/tx` 는 **RPC 를 직접** 읽는다. `data` 가 hex 원본이라 위 `decodeMemo`
  를 거쳐야 한다. 길이 제한이 없어 2048 바이트 초과 메모도 여기서는 원본을 볼 수 있다.

CORS 는 두 도메인 모두 `Access-Control-Allow-Origin: *` 이라 브라우저 확장에서
바로 호출된다 `[실측]`.

### 응답 예 (`/api/v1/address/{addr}/txs`)

```json
{
  "address": "0x…",
  "transactions": [
    {
      "hash": "0x…",
      "block_number": 1139989,
      "from": "0x…", "to": "0x…",
      "value": "1000000000000000000",
      "timestamp": 1785…,
      "status": 1,
      "memo": "2026년 용역계약서 3차 대금",
      "input_size": 39,
      "input_data": "0x323032366eb…"
    }
  ],
  "total": 12, "page": 0, "limit": 20
}
```

`memo` 가 `null` 이면 메모가 아닌 tx(일반 송금 또는 컨트랙트 호출)다.

***

## 6. 화면에 띄울 때 — XSS

**메모는 체인에서 온 임의 문자열이다.** 누구든 트랜잭션 하나로 여기에 무엇이든 써
넣을 수 있다. 이스케이프 없이 `innerHTML` 에 넣으면 그 사람이 벼린 사용자 모두의
화면에서 스크립트를 돌린다.

순서가 중요하다 — **이스케이프 → 링크 변환**. 뒤집으면 이스케이프가 자기가 만든
`<a>` 태그까지 먹어서 링크가 깨진다.

```js
const escaped = String(memo).replace(/[&<>"']/g, (c) => ({
  '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;',
})[c]);

const linked = escaped.replace(
  /(https?:\/\/[^\s<]+)/g,
  (u) => `<a href="${u}" target="_blank" rel="noopener noreferrer">${u}</a>`,
);

const html = linked.replace(/\n/g, '<br>');
```

`http`/`https` 만 링크로 만든다. `javascript:` 같은 스킴은 대상이 아니다.

React 등에서 텍스트로만 렌더링한다면 이스케이프는 프레임워크가 하므로 링크 변환만
따로 처리하면 된다.

***

## 7. 함정

**받는 쪽이 컨트랙트면 메모를 넣지 마라.** 컨트랙트 주소로 보내는 tx 의 `data` 는
함수 호출로 해석된다. 메모 바이트가 우연히 어떤 selector 와 겹치면 의도치 않은
함수가 불린다. 메모는 **EOA 수신자에게만** 붙인다. 받는 주소가 컨트랙트인지는
`eth_getCode` 로 확인한다 (`0x` 면 EOA).

**과거 트랜잭션에는 메모가 없다.** 인덱서는 배포 시점 이후 블록부터 `memo` 를
채운다. 소급 적용은 하지 않기로 했으므로, 그 이전 tx 는 `memo` 가 전부 `null` 이다.

**`input_data`** **는 여전히 74자에서 잘린다.** 함수 selector 확인용 필드다. 메모를
읽을 때 이 필드를 쓰면 안 된다 — `memo` 를 써야 한다.

**빈** **`data`(`0x`)는 그냥 일반 송금이다.** 메모 없는 송금이 기존과 똑같이 동작해야
하므로, 메모 입력란이 비면 `data` 자체를 넣지 않는다.

***

## 8. 확인하지 못한 것

* **실제 메모 tx 로 끝까지 확인하지 못했다** `[미확인]`. 자산 이동은 하지 않았다.
  파이프라인(판정 로직·스키마·API 응답·화면 코드·XSS 순서)은 8개 중 7개 검증했고,
  나머지 1개가 "메모 달린 실제 tx" 표본이다. 벼린에서 한 건 보내면 바로 확인된다.

* **노드 txpool 의 트랜잭션 최대 크기** `[미확인]`. 2048 바이트는 그 한계와 무관하게
  안전한 크기이므로 실무에 영향은 없다.

* **벼린 저장소 코드** `[미확인]`. 열어보지 않았다. 위 내용은 인터페이스 계약이다.

***

## 9. 서버 쪽 구현 위치 (참고)

| 파일                          | 역할                                   |
| --------------------------- | ------------------------------------ |
| `wallet-api/memo.js`        | 메모 판정 (`parseMemo`). 이 문서 2절 규칙의 원본  |
| `wallet-api/memo.test.js`   | 판정 시험 19건                            |
| `wallet-api/db.js`          | `memo` / `input_size` 컬럼 + 멱등 마이그레이션 |
| `wallet-api/indexer.js`     | 블록 인덱싱 시 `parseMemo` 호출              |
| `explorer/public/js/app.js` | 활동 내역 메모 행 + `memoHtml()`            |

