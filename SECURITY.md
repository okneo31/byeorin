# 보안 (Security)

벼린은 비수탁 지갑입니다. 사용자의 시드는 사용자 기기 밖으로 나가지 않으며,
우리는 그것을 복구해 줄 수 없습니다. 그래서 취약점 보고는 실제 자산 손실과
직결됩니다.

## 취약점 신고

**공개 이슈로 올리지 마세요.** 패치 전에 공개되면 그 사이에 실제 피해가 납니다.

- 메일: **okneojjjajh@gmail.com** (제목에 `[SECURITY]`)
- GitHub Security Advisory (비공개): 이 저장소의 Security 탭 → Report a vulnerability

포함해 주시면 좋은 것: 재현 절차, 영향 범위, 대상 버전(앱 푸터의 `v0.5.2 (3)`
같은 값), 가능하면 PoC.

첫 응답은 **72시간 이내**를 목표로 합니다. 수정과 공개 일정은 신고자와 협의합니다.

## 신고 대상 (in scope)

- 시드·개인키가 의도치 않게 노출되거나 유출되는 모든 경로
- 금고(`apps/android/src/keystore-session.ts`, `vault-hw.ts`) 우회 — 비밀번호
  없이 열리거나, 하드웨어 바인딩이 무력화되는 경우
- 서명 대상과 화면 표시가 다른 경우 (사용자가 본 것과 다른 트랜잭션이 서명됨)
- 주소 파생 오류 — 같은 시드에서 다른 지갑과 다른 주소가 나오는 경우
- 릴리스 검증 우회 — 위조 APK 가 `scripts/verify-byeorin-apk.mjs` 를 통과하는 경우
- 의존성 공급망 문제

## 이미 알고 있는 한계 (out of scope)

아래는 취약점이 아니라 **문서화된 설계 한계**입니다. 숨기지 않고 적어 둡니다.

- **칩 벤더 / OEM.** 금고는 AndroidKeyStore(TEE/StrongBox)에 묶여 있지만, 그
  펌웨어는 비공개이고 서명 주체가 벤더입니다. 벤더급 키 추출은 막지 못합니다.
  전례와 상세: [`apps/android/README.md`](apps/android/README.md)
- **물리 공격.** 폴트 인젝션·부채널·디캡.
- **잠금 해제된 상태의 단말에서 코드 실행.** 이 경우 TEE 에 복호화를 요청할 수
  있고, 다시 scrypt 대입 싸움이 됩니다.
- **재현 빌드 미보장.** 같은 커밋으로 빌드해도 바이트가 다릅니다(실측).
  즉 배포 바이너리가 이 소스에서 나왔다는 증명은 아직 없습니다 —
  [`docs/VERIFIABILITY.md`](docs/VERIFIABILITY.md) §2.1
- **사용자에 대한 공격.** 강압, 복구 문구 피싱, 클립보드 주소 바꿔치기.
  어떤 암호 기술도 막지 못합니다.

## 릴리스 진위 확인

받은 APK 가 진짜인지 우리를 믿지 않고 확인할 수 있습니다:

```sh
node scripts/verify-byeorin-apk.mjs 벼린.apk 벼린.apk.manifest.json
```

서명 인증서 지문 (이 값과 다르면 벼린이 만든 것이 아닙니다):

```
303f801bb44af8c494b6e89844fbe86c36bd6f48ab404a4b6c0228fa3f103480
```

## 아직 없는 것

- 버그 바운티 프로그램 (v1 GA 후 검토)
- 외부 감사 보고서

이 문서가 실제보다 안전해 보이게 쓰이지 않도록, 없는 것은 없다고 적습니다.
