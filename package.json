{
  "name": "nodong-wallet",
  "version": "0.0.0",
  "private": true,
  "description": "노동자의 지갑 — TTL 생태계 멀티체인 월릿 (SW + HW)",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20.10.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean",
    "setup-check": "node scripts/setup-check.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "pnpm": {
    "overrides": {
      "protobufjs": "^7.5.8",
      "axios@>=1.0.0 <1.15.2": "^1.15.2",
      "fast-xml-parser@<5.7.0": "^5.7.0"
    }
  }
}
