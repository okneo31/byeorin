{
  "name": "nodong-wallet",
  "version": "0.0.0",
  "private": true,
  "description": "노동자의 지갑 — TTL 생태계 멀티체인 월릿 (SW + HW)",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20.10.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
